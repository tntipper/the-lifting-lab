# Agent handover — The Lifting Lab integration

Updated: 2026-09-24. Verify Git and hosted state before acting. This is the current recovery entry point; historical evidence is linked below.

## Objective and release boundary

Complete the staged comparison-site/Shopify integration under `docs/ops/integration-release-candidate.md` and `docs/ops/project-stage-execution-protocol.md`. The immediate gate is Stage 3 staging account/cart acceptance. Production deployment, public launch, purchases, supplier orders and promotional customer messages are outside the current authorization. Every credential window, connected-branch push and provider change needs its own reviewed exact plan and action-time approval.

## Verified local and hosted checkpoint

- Repository: `implementation-integration`, branch `codex/tll-integration`. Check HEAD and status on entry. Preserve the six pre-existing unrelated untracked paths: `.agent/HANDOVER 2.md`, `.agent/gen11-journal-watch.mjs`, `config/staging-account-hosted-baseline-manifest 2.json`, `implementation-state/`, `scripts/staging-account-hosted-baseline-keychain 2.py`, and `scripts/staging-account-hosted-baseline-live-launcher 2.mjs`. The private `implementation-state/staging/` directory contains consumed one-use journals and must not be deleted or replayed.
- The owner-approved single Git publication pushed only immutable source `abd5a5258dd1072daf83a4447ce7110465f445e1` to remote `codex/tll-integration`. Its committed activation-manifest SHA-256 is `4f05bca4a90983ea8b5e72d23f5c5fe4e69110dfbb90ccde1ff1d53dcde6e5c3`; fixed Git source proof passed. The local-only arming commit `e8a68325b7d87b7878d95b88f984d1d7743f1683` was never pushed and was disarmed at `609d5e4`. Do not repeat publication. See `docs/ops/evidence/2026-09-24-stage3-staging-preview-publication.md`.
- Vercel project `prj_kI5iqqor8Qa63EGRyhsi8e2yxpg4` showed protected Ready Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` at that source. A separately approved one-use read of the branch alias, immutable URL and alias again returned `RUNTIME_DISABLED_VERIFIED`: all four private/public account/cart flags were boolean false. V2 journal run `e95b04f0-bdcc-434e-8b8f-3c429cf8ac54` is terminal and mode 0600. This proves the disabled runtime only, not sign-in or checkout. See `docs/ops/evidence/2026-09-24-stage3-preview-disabled-runtime-result.md`.
- An earlier V1 runtime journal was consumed by a faulty offline harness before any Keychain or network access. Preserve it as terminal HOLD; do not retry it. Root cause and recovery are in `docs/ops/evidence/2026-09-24-preview-runtime-proof-v1-offline-harness-incident.md`. Astra's later independent audit found that the original private V2 offline harness isolated its journal but retained live credential/network calls if the tested guard regressed. A new private safety harness `../implementation-state/staging/preview-runtime-proof-safety-offline-v3.py` removes both native ports from a temporary copy before testing expired, unexpired and replay paths; it returned `ISOLATED_V2_DEADLINE_AND_REPLAY_PASS`. Preserve historical scripts and receipts. Do not reuse the older V2 harness pattern.
- Supabase staging project `qdmvngjwkcsilzmqksme` still lists `custom:tll-staging-subject-broker-v1` ENABLED in the signed-in dashboard, with only three CA/Edge-flag custom Edge secret names and no broker secret name. The approved V2 official read-only probe has now independently verified the full provider and strict database preconditions; see the result below. Production project `wrhgscovsgsudtedbljr` is excluded.
- The disabled provider-normalization launcher was corrected to use the available bundled Python because `/usr/bin/python3` is blocked by an unaccepted Xcode license on this Mac. Both launcher and helper gates remain false. Focused checks 24/24, manifest, live-boundary and diff checks passed; Astra independently found no issue in that scoped change. Before any armed window, verify the bundled executable path and SHA-256 `ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`. See `docs/ops/stage-plans/2026-09-24-stage3-provider-python-executable-recovery.md`.

## Next action — Stage 3 provider normalization review gate

**Latest disabled work unit (24 September):** The native legacy-login-Keychain reader has been written, compiled in a private disabled build, independently reviewed and verified offline. It is not connected to a launcher and has never accessed the real items. The full suite passed 2,447/2,449 tests (2 skipped), with typecheck, targeted lint, activation manifest, live-boundary, private build identity and diff checks passing. Read `docs/ops/evidence/2026-09-24-stage3-disabled-native-keychain-reader.md` and `docs/ops/stage-plans/2026-09-24-stage3-noninteractive-keychain-reader.md`. Source SHA is `46de62172b91f8258dd1ee1b109b984e1134c9c30416d83e6fb604e197b6daf4`; private disabled binary SHA is `92e4e3a57e51af29ce359bc19133e811f08857414c3e769c4b152fe1b0f5cad2`. **Next coherent unit:** make a separate reviewed plan for disposable, non-secret Keychain fixture qualification. Do not invoke this binary against real items, change their ACLs, integrate a launcher or arm provider mutation on the basis of the disabled tests. That later integration needs parent deadline and private-pipe failure controls. Incident gate remains OPEN/HOLD.

**Current HOLD (24 September):** The approved one-shot provider-normalization launcher was **not invoked**. Its exact Python Keychain reader failed in a precheck after one direct shell read had succeeded; the helper hid the underlying error at that time. Both mutation journals remain absent. The sibling normalization worktree was disarmed at `c08633e300ab22b8cc711c42d331bc0f7c1b1391`, with all nine gates false. A one-hour, project-scoped Vercel token was briefly saved in Keychain Comments and appeared in a local CUA output; Comments and clipboard were cleared, and the token was left to expire naturally. See `docs/ops/evidence/2026-09-24-stage3-provider-credential-storage-incident.md`. **Next coherent unit:** independently investigate why the earlier exact helper failed while the later bounded all-selector diagnostic passed; establish a supported, deterministic credential-access control or direct cause and review it under the incident-learning gate. Do not rerun the consumed readiness receipt, reuse historical arming commits, create a token merely to retry, or proceed to provider mutation based on one PASS. Once the incident gate is satisfied, refresh hosted state and review a new provider-mutation plan and exact diff before seeking separate action-time approval.

Diagnostic follow-up: the helper now distinguishes six fixed, secret-free failure categories. Its 7 offline isolated tests, manifest, live-boundary, focused session tests and typecheck passed; an independent reviewer returned GO for this repair only. Direct secret-suppressed reads later passed under normal and restricted environments, but did not explain the earlier helper failure. See `docs/ops/stage-plans/2026-09-24-stage3-credential-readiness-diagnostic.md`. The incident remains OPEN/HOLD.

The all-selector readiness process is now implemented **disabled** with a separate mode-0600 phase receipt and a 50-second absolute deadline; both new native gates are false after the completed one-run diagnostic. The injected offline tests cover ordered reads, deadline/slow-start failures, secret-buffer wiping, receipt replay refusal and native process parameters. Independent Astra review returned GO for the disabled implementation after deadline corrections. Full `npm test` passed before arming (2,444 pass, 2 skip, 0 fail); typecheck, activation manifest and live-boundary checks passed. A pre-existing hosted-baseline manifest hash was corrected to match the unchanged source file committed in `be38e13`; independent review returned GO for that single-hash correction. The incident remains OPEN/HOLD because the earlier intermittent Keychain failure is unexplained despite the bounded all-selector PASS.

Local preparation after commit `244f744`: a new, still-disarmed sibling checkout exists at `../implementation-provider-credential-readiness-arm-v1` on branch `codex/tll-provider-credential-readiness-arm-v1`, at that exact commit. All three fixed Keychain item metadata lookups returned present without requesting secret values. The shared `../implementation-state` parent was mode 0755 and would have blocked the new launcher; it was tightened to owner-only 0700 and verified, as was its `staging` child. Both provider-mutation receipts and the readiness receipt were absent. No token validity, ACL, hosted state or live Keychain read was proved. Do not infer readiness or arm from this metadata alone.

The local readiness action plan and incident wording were corrected after an independent execution review. A retained expired Vercel item was sufficient to test **local Keychain readability**, but could not prove API validity; this diagnostic used no new token or hosted baseline. The exact arming preview `/tmp/tll-credential-readiness-arming-preview-v1.patch` was applied once and later reversed in the isolated sibling: two source switches and their two activation-manifest hashes, three files total. The shared helper also serves the normalization and provider-readonly launchers, which remained disabled; direct helper invocation was prohibited because it outputs credential bytes. The consumed readiness receipt cannot be retried. The incident remains OPEN/HOLD despite the PASS.

The owner approved that one local diagnostic, and it returned terminal PASS from the exact helper. The private one-use receipt `../implementation-state/staging/tll-provider-credential-readiness-v1.json` is now consumed and must not be replayed. Both mutation receipts remain absent. The local readiness sibling was armed at `f5ef2d19a2cebaf62ddc2753ea507cf65fa9bda3`, run once, then disarmed at `086839dde2e2472edd72d82f8d6a1d0713cbd3a0`; it is clean and disabled, and no commit was pushed. See `docs/ops/evidence/2026-09-24-stage3-credential-readiness-v1-result.md`. This proves local readability only; the earlier intermittent failure and API validity remain unexplained, so Stage 3 provider mutation is still HOLD.

Independent incident review confirmed the earlier empty-password stdin provisioning and token-in-Comments error, but found no supported cause for the later exact-helper failure. The `/usr/bin/security` invocation and timeout did not change before the readiness PASS; exit-code classification was diagnostic, not a repair. The disabled native-reader source/build/injected-test part is now complete; its runtime behavior with a disposable non-secret item and any exact-item ACL change remain separate gates. Do not treat the implementation as incident closure. Apple distinguishes the file-based login keychain from the data-protection keychain; the reader targets the former and must be qualified on this Mac.

The owner-approved V2 official provider/database/secret preflight ran **once** from local-only arming commit `4e53ae4` and returned `READONLY_PROVIDER_PRECONDITION_VERIFIED` with strict database receipt hash `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`. The provider is still enabled with JWKS present, the broker secret name was absent and strict staging database invariants passed. The V2 private journal `../implementation-state/staging/tll-provider-readonly-probe-v2.json` is terminal `STOPPED_BEFORE_UPDATE` and must never be replayed. The sibling worktree was disarmed at `1b9e532`; its arming paths are byte-for-byte equal to disabled base `40b1654`, and native boundaries passed. See `docs/ops/evidence/2026-09-24-stage3-provider-readonly-refresh-v2-result.md`.

The eventual provider mutation remains limited to two fields (`enabled:false`, `jwks_uri:''`) and one update attempt, with in-session official full-field readback and separate categorical reconciliation. Customer/cart/Edge activation remains off until full Stage 3 journey acceptance.

24 September continuation: independent Astra review found and we repaired a Preview identity gap before arming: the normalization preflight now pins deployment `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b`, immutable URL and source `abd5a5258dd1072daf83a4447ce7110465f445e1`; its surface reader rejects changed identity before sending the retained bypass and checks alias again afterward. The disabled repair is `be38e13d34a1883aafe30619a631e066a1801086`. Focused tests 49/49, activation manifest, live-boundary, typecheck, targeted lint and diff checks passed. Independent rereview returned GO for the disabled repair. The exact nine-gate arming diff was independently reviewed and committed **locally only** in fixed sibling `../implementation-provider-normalization-arm-v1` at `ecfe6d1460b68d460b43601331f85e8225e953bd`; it was never invoked or pushed and was later disarmed. Dependency imports resolve and bundled Python hash matches. Last dashboards showed the provider enabled, three expected Edge custom secret names and the same Ready Preview source; those reads are now stale. See `docs/ops/stage-plans/2026-09-24-stage3-provider-normalization-one-shot-gate.md`.

After Stage 3, the release plan still requires verified unified account/cart/orders/logout, inventory and order workers, supplier commercial/VAT basis, retail pricing and flash sales, affiliate tracking/discount/refund accounting, consent/email operations, research-backed scores, theme/customer journey consistency, backup/restore and production release checks. The offline pricing/affiliate modules are not connected to live checkout or payouts. Product-level score verification awaits source labels/Path A evidence.
