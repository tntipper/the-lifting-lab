# Agent handover — The Lifting Lab integration

Updated: 2026-09-25. Verify Git and hosted state before acting. This is the current recovery entry point; historical evidence is linked below.

**Next Stage 3 gate (25 September):** At local compatibility source `adda1f54d060a4564325d5e5ae5109e812ec56de`, remote `codex/tll-integration` is still `abd5a5258dd1072daf83a4447ce7110465f445e1`. The signed-in Supabase editor shows the custom provider disabled with its exact retained JWKS address; Vercel shows protected Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` Ready from the remote source. These are UI observations, not a new official provider or protected-runtime receipt. Local `npm run build` passed. An independent publication review found no actionable defect in the 72-commit candidate gap but advised against an automatic push: the old publisher is unselected and its journal consumed. The [next broker-rotation gate](../docs/ops/stage-plans/2026-09-25-stage3-next-broker-rotation-gate.md) now includes full provider prechecks, a durable per-effect phase record and bounded cleanup/reconciliation after an independent review identified those missing controls; rereview returned GO for disabled preparation only. **Next:** build and verify that fresh bounded package while all live gates remain off; obtain a new official staging/protected Preview baseline and separate action-time approval only after independent review of the implementation and arming diff. Do not repeat the failed JWKS-clearing update, reuse journals, publish automatically, or activate account/cart. Preserve the six unrelated untracked paths. Older "next" directions below are historical where they disagree.

**Current Stage 3 development checkpoint (25 September):** The disabled future-rotation and hosted-baseline source now recognise only the exact JWKS address observed in the official safe-held read. Future rotation omits `jwks_uri` from its update; unknown/blank/near-match addresses stop before staging credentials, and changed post-update readback requires reconciliation. The hosted-baseline receipt is v2 and independently detects address or discovery-document drift. The old failed normalization contract and consumed journals are unchanged. Independent security review returned GO for this disabled preparation; focused tests passed 94/94, the full suite 2,573 passed/2 skipped, typecheck, targeted lint, both manifest checks and live-boundary check passed. See the [compatibility plan](../docs/ops/stage-plans/2026-09-25-stage3-retained-jwks-compatibility.md). **Next:** verify this committed diff and fresh hosted staging/Preview state, then independently review a bounded future-rotation plan before requesting separate action-time approval. No credential or hosted API was touched by this source change. Provider, account and cart activation remain HOLD. Preserve the six unrelated untracked paths. Historical paragraphs below are superseded where they disagree.

**Latest Stage 3 result (25 September):** The independently reviewed, owner-approved official read-only provider reconciliation ran once and returned `SAFE_HELD_PROVIDER_OBSERVED`: staging provider disabled with the exact known retained JWKS address and pinned sign-in settings; database controls off; broker secret name absent. Its new private journal is terminal `STOPPED_BEFORE_UPDATE`, and the isolated checkout was immediately disarmed with manifest/live-boundary checks passing. See [one-run evidence](../docs/ops/evidence/2026-09-25-stage3-provider-safe-held-read-v1.md). This establishes a **safe holding state**, not successful two-field normalization or historical proof that all fields were unchanged. The old mutation journals and this read journal are consumed; never replay them. Future broker-rotation/baseline code still demands an empty JWKS URL, so account/cart activation remains HOLD. Next: independently assess the smallest compatible change to those future checks, keeping disabled state and current safety controls intact; do not mutate the provider under the old plan. Historical paragraphs below are superseded where they disagree.

**Stage 3 incident (25 September):** The owner-approved Supabase-only provider attempt made one update and returned `RECONCILIATION_REQUIRED`. Both mutation journals are consumed at `UPDATE_DISPATCH`. Supabase then showed the staging provider **DISABLED** with its old JWKS URI retained; the later official read-only check confirmed that current configuration. Published Supabase Auth server code ignores an empty JWKS update, which strongly explains the partial result, although the hosted server version is unknown. The original two-field acceptance condition did **not** pass. Do not replay the update, re-enable, delete/recreate, edit Auth tables, or activate account/cart. See the [incident and next gate](../docs/ops/evidence/2026-09-25-stage3-provider-two-field-incident.md).

**Read-only reconciliation preparation (completed 25 September):** The safe-held observer and launcher target only the current staging provider, database controls and broker secret names. They use a distinct one-use journal and contain no provider update call. An independent review found and resolved a cumulative timeout mismatch; database and secret-name reads share one 30-second budget. Eight focused tests, including a virtual-clock deadline test, passed. The exact minimal arming diff was independently reviewed, the owner approved one read, and the result is recorded above. See [stage plan](../docs/ops/stage-plans/2026-09-25-stage3-provider-safe-held-read.md).

**Newest Stage 3 checkpoint (25 September):** The disabled Supabase-only provider session/launcher and protected Preview readiness observer/launcher are implemented with separate one-use journals. Focused tests cover one provider update, strict full-field readback, replay prevention, three fixed protected Preview GETs, alias drift, response rejection and a 10-second timeout. The generated activation manifest, targeted lint, typecheck and live-boundary check pass. Independent security review gave GO for disabled preparation and for the exact Preview-only arming diff. The owner-approved protected Preview read ran once and returned `PREVIEW_DISABLED_VERIFIED` for pinned deployment `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b`; all four account/cart switches were false. Its [private journal and evidence](../docs/ops/evidence/2026-09-25-stage3-protected-preview-readiness-v1.md) are terminal/consumed. The isolated Preview checkout was disarmed, manifest/live-boundary checks passed and its tracked tree is clean. The provider remains enabled. **Next:** prepare/review the separate Supabase-only provider arming diff, obtain a fresh read-only staging baseline and action-time owner approval before one two-field update. Both live launchers and the shared Keychain reader are currently **OFF**. See [stage plan](../docs/ops/stage-plans/2026-09-25-stage3-supabase-only-provider-disable.md). Preserve the six pre-existing unrelated untracked paths. Historical checkpoint paragraphs below are superseded where they disagree.

**Provider attempt follow-up (25 September):** The exact three-file Supabase-only arming diff was independently reviewed and approved for an action-time decision; the owner approved one run. That run stopped immediately before Keychain access or any provider journal because the isolated worktree lacked `node_modules`. This was confirmed by offline `ERR_MODULE_NOT_FOUND` and corrected locally by linking its dependencies to the main checkout, then checking every dynamic import offline. No live retry was made. Both provider journals remain absent and the provider was still enabled at the pre-run UI observation. The isolated provider checkout is disarmed and clean. Read the [incident and prevention](../docs/ops/evidence/2026-09-25-stage3-provider-local-runtime-stop.md). **Next:** refresh the baseline and exact patch review, then obtain a new action-time approval for any provider run. Never treat the previous approval as reusable.

**Current checkpoint — Stage 3 provider gate reassessed, HOLD (25 September):** The V2 synthetic fixture cleanup is terminal PASS and disarmed; do not rerun V1/V2 or reapply the consumed patch. Its [one-run evidence](../docs/ops/evidence/2026-09-25-stage3-fixture-recovery-v2-one-run-result.md) remains private and intact. The [current provider-gate assessment](../docs/ops/evidence/2026-09-25-stage3-provider-gate-reassessment.md) found both provider-mutation journals absent, all provider-normalisation controls disabled, manifest/live-boundary checks passing, focused provider/native-reader tests 37/37, and the bundled Python executable still pinned. The exact Python credential helper's intermittent failure remains unexplained and the native alternative is unqualified. The [access-route reassessment](../docs/ops/evidence/2026-09-25-stage3-access-route-reassessment.md) parks the large disposable-reader qualification because a synthetic PASS would still not prove real-item access. A newly connected Chrome profile provided the [fresh dashboard read and offline slice](../docs/ops/evidence/2026-09-25-stage3-fresh-dashboard-read.md): staging provider remains ENABLED; pinned Vercel Preview deployment/source is Ready; Supabase's editor requires JWKS and its separate Disable action cannot perform the reviewed two-field change with full-field proof; protected `/api/staging/readiness` was browser-blocked and its flags are unknown. A new injected-only Supabase coordinator is disabled; independent review returned GO for offline preparation, and eight focused tests plus targeted lint, typecheck, manifest and live-boundary checks pass. **Next:** prepare a bounded Supabase-only credential/transport launcher and distinct one-use journal; obtain a current protected Preview/flag observation, independent arming review and fresh action-time owner approval before any live mutation. Do not spend a credential window or mutation journal to diagnose access. Provider normalisation and account/cart activation remain HOLD. Preserve the six unrelated pre-existing untracked paths. Entries below are historical and their old “Next” instructions are superseded by this paragraph.

**Current Stage 3 recovery checkpoint (25 September):** The disabled V1 recovery source has an [offline-reviewed phase-local baseline correction](../docs/ops/evidence/2026-09-25-stage3-recovery-baseline-offline-correction.md): it now separates the native effective list from the explicit user list, preserves ordered paths and file identities within each phase, and rejects aliases of both preserved fixture files. Independent review returned GO **for offline preparation only** after a sidecar-alias test was added. The old V1 journal remains consumed and no live cleanup has run. **Whole-run preservation is still missing:** each child phase captures a fresh baseline, so changed entries between phases could be accepted. Next: design V2 with a private pinned run-start baseline checked across every phase and final reconciliation, test between-phase drift blocking later deletion, then independently review a distinct V2 one-use package. Do not arm V1, replay any consumed journal, remove fixture files or lift Stage 3 HOLD. Preserve the six unrelated untracked paths.

**Latest Stage 3 result (25 September):** The independently reviewed, owner-approved search-domain diagnostic ran **once** from disabled source commit `87ee056` and returned `DOMAIN_USER:CURRENT_2:USER_1:EXACT_1:SAMEFILE_1:USER_EXACT_1:LISTS_DIFFERENT:IDENTITY_COMPLETE`. Its new private journal is terminal/consumed and source/binary-bound. The native effective list contains the expected login Keychain plus one different file, while the explicit user list has only login. This means the old cleanup's single-entry effective-list guard would fail **today before deletion**; it does not prove the exact earlier failure point. The four-line arming patch was reversed, both gates are false, manifest/live-boundary and tracked diff pass, and the fixture/default/user-list metadata remained unchanged. See [full result and learning](../docs/ops/evidence/2026-09-25-stage3-search-domain-v1-result.md). Next: design and offline-test the smallest recovery correction that compares the complete effective and explicit user lists before/after while preserving unrelated entries; independently review before proposing any new cleanup window. **Do not replay either consumed diagnostic or the old cleanup journal, reapply the consumed patch, delete the fixture, normalise provider or activate account/cart. Stage 3 remains HOLD.** Preserve the six unrelated untracked paths.

**Latest Stage 3 result (25 September):** The independently reviewed, owner-approved preserved-fixture metadata diagnostic ran **once** and returned `SEARCH_PATH_MISMATCH`. Its private V1 journal is terminal/consumed, bound to the verified source and binary, and must never be replayed. The native default login-Keychain path matched; its readable current-domain search list was not exactly the expected one-element login list, while the `security` CLI's explicit user-domain list remained the expected sole login Keychain. The exact difference and old cleanup failure point are still unknown. The arming patch was reversed immediately; both gates are false, manifest/boundary pass, and postflight found the fixture directory/main/sidecar unchanged with no scratch remains. Private armed binary and receipt are preserved as evidence. Read [full result and learning](../docs/ops/evidence/2026-09-25-stage3-metadata-v1-result.md). Next: design a separate minimal read-only categorical check of native list count/membership/path equivalence and preference domain, independently review and obtain new action-time approval if necessary. **Do not weaken equality by guesswork, retry cleanup, normalize provider or activate account/cart. Stage 3 remains HOLD.** Preserve the six unrelated untracked paths and all old journals.

**Current Stage 3 gate (25 September):** Independent rereview of corrected disabled source `ffd509a` and its [revised arming record](../docs/ops/arming-previews/2026-09-25-stage3-metadata-ffd509a.md) returned GO **to fresh baseline and approval**, not execution. A read-only local baseline then passed: branch/HEAD/status, both switches false, private directory modes, absent new journal/binary/receipt, default/sole login Keychain, and descriptor-bound pinned fixture metadata. Read [evidence](../docs/ops/evidence/2026-09-25-stage3-metadata-review-and-baseline.md). The point-in-time baseline must be refreshed immediately before any run. Next required action is a fresh owner decision for the one-run diagnostic; if approved, use only the reviewed four-change proposal and fixed one-use launcher. No armed build or real diagnostic has run, the fixture and old journals are untouched, Stage 3 remains HOLD, and the six unrelated untracked paths remain preserved.

**Independent Stage 3 review correction (25 September):** Reviewer returned HOLD on the first disabled metadata runner because the launcher used pathname-based fixture child reads before its journal and the build checker read binary/receipt contents before confirming file identity. Both were corrected at `ffd509a`: only descriptor-bound native Swift reads fixture children after journalled dispatch; the builder validates opened files before bounded reads, verifies a private executable copy and strict signature, and the launcher executes only a private copy matching the reviewed binary digest. Focused tests, TypeScript, lint, manifest, live-boundary and full suite (2,508 pass, 2 skip) passed. The original arming preview is **superseded**. The [revised unapplied record](../docs/ops/arming-previews/2026-09-25-stage3-metadata-ffd509a.md) pins the corrected four changes; its temporary patch SHA-256 is `39aa4389087247e627ca40435817c11d9e96278bd08e81a5024be8165fa50d38`. Next: independent rereview, fresh baseline and action-time approval. All gates remain false; no real fixture/Keychain read or armed build occurred.

**Stage 3 next-gate preparation (25 September):** The separate [one-run metadata plan](../docs/ops/stage-plans/2026-09-25-stage3-fixture-metadata-one-shot.md) specifies a read-only diagnostic for the preserved synthetic fixture without reusing the terminal V1 cleanup journal. The disabled fixed-target build/check, dedicated one-use journal, injected coordinator and disabled launcher are now prepared, source-pinned and tested at `aa1834f`. Both Swift and launcher gates are false; the new journal, armed binary and build receipt are absent. Focused metadata and manifest tests, TypeScript, targeted lint, live-boundary and the full suite (2,506 pass, 2 skip) passed. The manifest test initially expected its old source list; that list was corrected and the full suite reran cleanly. The unapplied [arming-change record](../docs/ops/arming-previews/2026-09-25-stage3-metadata-aa1834f.md) has SHA-256 `b9b004fafe49444fc37569d7204964a2456785559a0566d55ca2f57f00b083d1` and passed `git apply --check`. **No live binary was built and no real fixture or Keychain metadata was read.** Next: independently review the disabled package and exact preview, refresh the local baseline, then seek fresh action-time owner approval for one diagnostic run. A `METADATA_MATCHED` result would rule out only two hypotheses; it would not lift the Stage 3 HOLD or authorize another cleanup. Preserve the six unrelated untracked paths and all prior journals.

**Latest verified local slices (25 September):** Calculator, compare, myths, combine and side-effects pages have had outdated public promises of 0–100 product scores, “best 2026” selections and automatic stack building replaced with accurate research/unassessed wording. The old product URLs and calculator functions remain intact. A second small slice aligned the shared navigation, home-page card and supporting article links with the existing research-only destinations. After each slice, TypeScript, targeted lint, live-boundary check and the full suite (2,495 pass, 2 skip) passed; first-slice lint had three pre-existing warnings and second-slice lint was clean. These are local source results only, not a live deployment. Further copy review should be bounded to genuinely visible claims, especially conditional category guides; do not rewrite their dormant approved-assessment branches merely for wording. Stage 3 remains HOLD. Preserve the six unrelated untracked paths.

**Stage 3 diagnostic preparation (25 September):** A separate [metadata-only diagnostic plan](../docs/ops/stage-plans/2026-09-25-stage3-fixture-metadata-diagnostic.md), disabled Swift source and offline test now classify default/search-list path and pinned synthetic-fixture metadata mismatches without reading Keychain items or deleting anything. The first independent review found a directory-replacement race and missing wrong-identity tests; both were corrected, and a second independent review returned PASS for disabled preparation. The real entry is hard-disabled; it has not been built as a live executable or run against the Mac's Keychain/fixture. The focused 16-case offline test, Swift typecheck, TypeScript and live-boundary checks passed; the final full suite passed 2,494 with two skipped. This work does not identify the old native failure, clear the consumed cleanup journal, or lift Stage 3 HOLD. Next: design a separately reviewed exact one-use read-only diagnostic window with source-bound build and fresh baseline before asking for action-time approval. Preserve the six unrelated untracked paths and all terminal journals.

**Latest owner launch decision (25 September):** Comparison products may stay visible as neutral or clearly unassessed while scientific reviews proceed. This supersedes the older all-229-assessed-before-opening requirement and resolves the pending question in the TropShip-first checkpoint below. Do not imply that an unassessed product is scientifically recommended. Current assessment code blocks rankings and recommendations, but still exposes some unapproved historical numeric scores as “Legacy score”; hide or clearly contain those numbers across public product, comparison and alternatives views before claiming the new launch rule is met. Preserve accurate label, cost, stock, customer-policy and tested-checkout gates for products TLL actually sells. See [current opening rule](../docs/ops/tropship-first-opening-and-data-work-2026-09-25.md) and [research programme](../docs/research/all-product-assessment-programme-2026-09-24.md). No live site change was made by recording this decision; Stage 3 HOLD remains.

**Neutral-display work (25 September):** Shared product cards, the all-products grid and product-detail card now show “Not assessed” or “Under review” instead of historical `/100` or percentage values. A second local slice withholds frozen numbers from the shared assessment projection, stack summaries/captions, social/share images, alternatives and matchup hubs, and the named public catalogue/detail/compare/favourites/stack/ARD JSON projections while preserving the frozen source and disabled approval gate. Focused checks 56/56, final full suite 2,495 pass/2 skip, TypeScript/targeted lint and synthetic browser journeys at six widths passed. Independent review found no regression in the changed surfaces, but identified old promotional wording on several pages and frozen numbers still present in browser page data/client bundles. The browser test initially expected the old average; that assertion was corrected to require no approved average, then passed. These are local code results only. The first promotional wording sweep is recorded above. Next: decide whether browser-data secrecy is actually a requirement (visible rating containment does not require it), and prove the exact deployed version before launch; no live deployment occurred.

**Mid-build reset checkpoint (24 September):** The limited [discovery delta](../docs/ops/mid-build-discovery-delta-2026-09-24.md) and [revised remaining-work plan](../docs/ops/revised-remaining-work-plan-2026-09-24.md) now govern remaining sequencing; the original plan remains the finding register. A small, disabled Stage 3 parent child-result classification is prepared and locally verified; see [evidence](../docs/ops/evidence/2026-09-24-mid-build-reset-and-child-classification.md). It does not explain the consumed native failure or lift the HOLD. Continue with independent review and a separate read-only native diagnostic plan. Preserve the terminal journal and fixture. Two owner decisions on first-opening scope (unassessed scores; manual pricing versus automation/affiliates) were requested and remain pending as of this checkpoint. Verify the current Git HEAD/status before further edits; this checkpoint was written over baseline `ea9dd7d8f8eab9577a3f90a485af9b14d73d594b` with only its named code/manifest/docs changes intended.

**Owner input and commercial correction (25 September):** The owner returned the completed decision form and TropShip documents. The earlier two launch questions are answered: all-product scientific assessment is required before opening; reviewed manual prices may open before competitor repricing, flash sales and affiliates. The account-specific TropShip welcome email contradicts the public wholesale threshold: £5+VAT is charged for each TropShip supplier order. The disabled local calculator and catalogue gate now budget £6 at every wholesale total under tariff v3; old/free projections fail closed. Full suite 2,493 pass, 2 skip; typecheck, targeted lint, activation manifest and live boundary pass. Read [evidence](../docs/ops/evidence/2026-09-25-owner-input-and-tropship-tariff-correction.md), [scientific programme](../docs/research/all-product-assessment-programme-2026-09-24.md), and [customer-service research](../docs/ops/customer-service-policy-research-2026-09-24.md). No supplier connection, Shopify price change or customer policy publication occurred. Confirm the supplier tariff before price publication and cost the proposed £100-retail free postage and affiliate rates. Stage 3 remains HOLD exactly as above. Preserve the uploaded SFTP credential outside Git and do not use it without a separately reviewed sync plan.

**Scientific register source check:** A public-anonymous export stopped locally before connecting because `.env.local` lacks a Supabase public URL/key. The signed-in production Supabase table editor showed **229 records** in `public.products` on 25 September. Three read-only SQL pages exported a [229-row identity snapshot](../docs/research/data/supabase-products-2026-09-25.csv) with unique sorted IDs and 25 category totals reconciled to the live count; see the [evidence note](../docs/ops/evidence/2026-09-25-owner-input-and-tropship-tariff-correction.md). This is not a Shopify variant, exact formula/label, or approved assessment count. The historical 230 score records are not a substitute. Next: reconcile Shopify exact variants and labels, then bounded formula/evidence batches. Do not claim product assessment beyond the identity inventory.

**Supplier/variant/offer checkpoint (25 September):** The 14 September account email confirms £5+VAT per TropShip supplier order and separately assigns £100 free delivery to wholesale accounts; public pages still lack account-specific detail, so written reconfirmation is required before price publication. The [read-only joined register](../docs/research/data/comparison-shopify-supplier-reconciliation-2026-09-25.csv) links 61/229 comparison IDs to 60 historical Shopify variants; 168 have no historical mapping, nine mapped variants are explicit supplier multipacks and 19 mapped rows lack feed nutrition text. An `APP449` manufacturer-page pilot found a plausible formula/size match, but no barcode-bound exact label/Shopify proof. The [conditional offer screen](../docs/ops/evidence/2026-09-25-supplier-variant-offer-screen.md) shows severe margin warnings for old prices under proposed free postage and 10% discount/commission; no offer was enabled. A copy-ready [supplier enquiry](../docs/ops/drafts/tropship-account-confirmation-2026-09-25.md) exists but was **not sent**. Chrome's signed-in Shopify browser connection timed out twice before any Shopify page could be read; do not claim a fresh Shopify reconciliation. Next: obtain fresh Shopify variant export/read-only access and current supplier price/VAT/pack confirmation, then approve exact variant costs and rerun the margin engine. Preserve Stage 3 HOLD and all existing untracked files.

**Current Shopify export addendum (25 September, later):** The earlier browser/export gap is now closed. A fresh Shopify Admin CSV arrived in Downloads; the two copies are byte-identical. The [curated current 98-variant screen](../docs/research/data/shopify-current-variant-screen-2026-09-25.csv) and [evidence/addendum](../docs/ops/evidence/2026-09-25-current-shopify-variant-and-offer-addendum.md) verify 87 products, 98 variants, all 60 historical mapped variants still present at the same listed prices, 74 old-feed-linked SKUs, 10 multipacks, and only 12 populated Shopify barcodes (all old-feed matches). Standard export omits numeric variant IDs and does not prove current supplier stock or labels. Thirteen active published £0 placeholders lack a feed link; 12 active ISO-XP variants have zero tracked stock with selling-past-zero enabled. Check their non-purchase storefront path and correct deliberately before promotion. Of 64 feed-linked non-multipacks, 55 warn under the proposed 10% customer discount plus 10% first-order commission even if old wholesale already includes VAT and payment/returns costs are zero (60 warn if VAT is extra). This is a conditional screen, not an approved price decision. Supplier enquiry remains unsent, supplier VAT/pack/fee confirmation and exact labels remain open, customer free delivery/affiliate offers remain disabled, and Stage 3 remains HOLD. Do not repeat the Shopify export unless it changes or a fresh launch gate requires it. Preserve all unrelated untracked files.

**TropShip-first direction and data-swarm pilot (25 September):** Owner now prioritises a small approved TropShip opening range, reviewed outbound affiliate links for gaps, and later wholesale stock for proven demand. The [TropShip-first work note](../docs/ops/tropship-first-opening-and-data-work-2026-09-25.md) explains the launch barrier split; the [74-SKU research queue](../docs/research/data/tropship-first-evidence-queue-2026-09-25.csv) is investigation-only, not sellable approval. Two read-only workers separately inspected manufacturer evidence and local supplier/Shopify fields. The 14 Sep feed and Shopify export have **no RRP**; 20/74 feed-linked SKUs have no nutrition text. Four of ten pilot SKUs had material supplier-versus-manufacturer flavour/formula problems, so do not auto-publish supplier nutrition. The [expanded account enquiry](../docs/ops/drafts/tropship-account-confirmation-2026-09-25.md) asks Tropicana for tariff, VAT, RRP, exact labels, stock and fulfilment; it is **not sent**. Shopify product status occurs only on the first row per handle; carry it over before counting active variants (61/74 feed-linked active). The owner has been asked whether the earlier all-229-scientific-assessment-before-launch rule is relaxed for neutral unassessed products; until answered, preserve the stricter rule. RRP, broad automation and full promotional offers may be parked for a manually reviewed opening range; exact sold-unit, actual landed cost, accurate displayed facts, stock and checkout acceptance may not. Next: resolve the scientific opening-scope answer when received, progress independent Stage 3 customer-flow work, and use the supplier reply to approve a small sellable SKU set. Preserve all unrelated untracked files and Stage 3 HOLD.

**Signed-in Tropicana website read (25 September):** The owner permitted read-only Safari navigation; [site evidence](../docs/ops/evidence/2026-09-25-signed-in-tropship-site-read.md) confirms that signed-in account pages already contain the TropShip order API documentation and product pages expose prices, stock, nutrition and barcodes. For `APP449`, the page showed £7.45 + VAT / £8.94 inclusive, matching the 14 Sep feed's £7.45; that supports **ex-VAT for this SKU only**, not a blanket feed declaration. The account email's £6-per-order TropShip tariff remains distinct from generic website free-above-£100 delivery wording. The [supplier email draft](../docs/ops/drafts/tropship-account-confirmation-2026-09-25.md) was narrowed to account-specific and unverifiable questions and remains **unsent**. The owner reports that Tropicana image-use permission was already confirmed, so it has been removed from the enquiry; preserve the prior confirmation rather than asking again. No basket item, order, payment, API call, credential access or setting change occurred. Preserve all Stage 3 holds.

## Objective and release boundary

Complete the staged comparison-site/Shopify integration under `docs/ops/integration-release-candidate.md` and `docs/ops/project-stage-execution-protocol.md`. The immediate gate is Stage 3 staging account/cart acceptance. Production deployment, public launch, purchases, supplier orders and promotional customer messages are outside the current authorization. Every credential window, connected-branch push and provider change needs its own reviewed exact plan and action-time approval.

## Verified local and hosted checkpoint

- Repository: `implementation-integration`, branch `codex/tll-integration`. Check HEAD and status on entry. Preserve the six pre-existing unrelated untracked paths: `.agent/HANDOVER 2.md`, `.agent/gen11-journal-watch.mjs`, `config/staging-account-hosted-baseline-manifest 2.json`, `implementation-state/`, `scripts/staging-account-hosted-baseline-keychain 2.py`, and `scripts/staging-account-hosted-baseline-live-launcher 2.mjs`. The private `implementation-state/staging/` directory contains consumed one-use journals and must not be deleted or replayed.
- The owner-approved single Git publication pushed only immutable source `abd5a5258dd1072daf83a4447ce7110465f445e1` to remote `codex/tll-integration`. Its committed activation-manifest SHA-256 is `4f05bca4a90983ea8b5e72d23f5c5fe4e69110dfbb90ccde1ff1d53dcde6e5c3`; fixed Git source proof passed. The local-only arming commit `e8a68325b7d87b7878d95b88f984d1d7743f1683` was never pushed and was disarmed at `609d5e4`. Do not repeat publication. See `docs/ops/evidence/2026-09-24-stage3-staging-preview-publication.md`.
- Vercel project `prj_kI5iqqor8Qa63EGRyhsi8e2yxpg4` showed protected Ready Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` at that source. A separately approved one-use read of the branch alias, immutable URL and alias again returned `RUNTIME_DISABLED_VERIFIED`: all four private/public account/cart flags were boolean false. V2 journal run `e95b04f0-bdcc-434e-8b8f-3c429cf8ac54` is terminal and mode 0600. This proves the disabled runtime only, not sign-in or checkout. See `docs/ops/evidence/2026-09-24-stage3-preview-disabled-runtime-result.md`.
- An earlier V1 runtime journal was consumed by a faulty offline harness before any Keychain or network access. Preserve it as terminal HOLD; do not retry it. Root cause and recovery are in `docs/ops/evidence/2026-09-24-preview-runtime-proof-v1-offline-harness-incident.md`. Astra's later independent audit found that the original private V2 offline harness isolated its journal but retained live credential/network calls if the tested guard regressed. A new private safety harness `../implementation-state/staging/preview-runtime-proof-safety-offline-v3.py` removes both native ports from a temporary copy before testing expired, unexpired and replay paths; it returned `ISOLATED_V2_DEADLINE_AND_REPLAY_PASS`. Preserve historical scripts and receipts. Do not reuse the older V2 harness pattern.
- Supabase staging project `qdmvngjwkcsilzmqksme` still lists `custom:tll-staging-subject-broker-v1` ENABLED in the signed-in dashboard, with only three CA/Edge-flag custom Edge secret names and no broker secret name. The approved V2 official read-only probe has now independently verified the full provider and strict database preconditions; see the result below. Production project `wrhgscovsgsudtedbljr` is excluded.
- The disabled provider-normalization launcher was corrected to use the available bundled Python because `/usr/bin/python3` is blocked by an unaccepted Xcode license on this Mac. Both launcher and helper gates remain false. Focused checks 24/24, manifest, live-boundary and diff checks passed; Astra independently found no issue in that scoped change. Before any armed window, verify the bundled executable path and SHA-256 `ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`. See `docs/ops/stage-plans/2026-09-24-stage3-provider-python-executable-recovery.md`.

## Next action — recover the synthetic fixture incident before provider normalization

**LATEST — cleanup attempt stopped UNCERTAIN (24 September, main HEAD `f1f40bd`):** The owner-approved identity-bound synthetic cleanup ran **once** from the correct main checkout after exact local/binary checks. It returned `UNCERTAIN/CHILD` in 0.58 seconds; new one-use journal `../implementation-state/staging/tll-provider-keychain-fixture-recovery-v1.json`, run `85e3c7f3-ce62-4081-bf63-7084695a68c9`, is terminal `API_DELETE/UNCERTAIN` sequence 2. The arming commit `2d49dcf` was immediately reverted at `f1f40bd`; boundary and manifest checks pass disabled. Read-only reconciliation shows the main synthetic Keychain, empty sidecar and directory unchanged at exact identities; default/search list remain the login Keychain. **Do not replay, delete manually or start another cleanup.** Independent Astra review found the native failure's root cause unproved because the launcher collapsed all child failures to one category; a normal timeout is not supported. Read `docs/ops/evidence/2026-09-24-stage3-fixture-v1-recovery-uncertain-incident.md`. **Next:** plan, implement, test and independently review a metadata-only native diagnostic plus fixed secret-free failure categories/injected deletion tests before any new deletion plan. Stage 3 remains HOLD.

**Historical arming preview — superseded by the UNCERTAIN result above (24 September):** The exact private arming patch `../implementation-state/staging/tll-fixture-recovery-arm-v1.patch` has SHA-256 `06f55f69798b6aa482d797928d1670bcb96bb751026b503234c62006a14c6da4` and changes only the two recovery gates and their two activation-manifest hashes. In an isolated sibling it produced an armed binary SHA-256 `3771b2278bf15a205c879e86c5e2dc4a973098e4344304787a151cf304b9715f`; Astra independently verified the binary/receipt/source/signature and the patch's minimality. The sibling was then disarmed and is clean. **Never execute there:** the native helper requires cwd basename `implementation-integration`. The main checkout remains disabled; the new recovery journal is absent. The armed binary and receipt are intentionally retained in shared private state and cannot be rebuilt over. Read `docs/ops/evidence/2026-09-24-stage3-fixture-v1-arming-preview.md` and the revised identity-bound plan. **Next:** refresh exact fixture/journal/login-Keychain identity, verify main HEAD and `git apply --check`, seek owner action-time approval, then apply the patch and `--check-armed` in the main checkout, run the launcher once only, disarm and reconcile. No live cleanup approval has been given yet.

**Disabled recovery ready for arming review (24 September):** A dedicated identity-bound V1 cleanup implementation is complete with both live gates false. Independent Astra review returned GO for disabled preparation after correcting deadline renewal, descriptor hashing, native authorization, sidecar entry checks, test isolation, per-child time reservation and Keychain interaction restoration. `npm test` passed 2492 with 2 skipped; typecheck, targeted lint, activation manifest, live boundary, disabled Swift compile and source-bound binary checks passed. The disabled source SHA-256 is `243e3523b72775406eeb367e27101389c3f457b1df5ba34523981d5a868d668a`, binary SHA-256 `0ed06c5dc5edcf352a559229efdaf2bd74939b14cd74184c56c5070eb67be4a7`. The earlier disabled binary and receipt are archived under `.retired-54df4e0c`. A fresh read-only inventory matched the exact leftover fixture and terminal V1 journals; the recovery journal is absent. The exact two-gate/two-hash patch is at `../implementation-state/staging/tll-fixture-recovery-arm-v1.patch`; Astra confirmed minimality but caught that the native helper's cwd guard forbids live execution from the sibling preview checkout. **Next:** verify the armed artifact in an isolated checkout only, then disarm that checkout. Refresh the fixture identity and obtain owner action-time approval before applying and running the reviewed patch in `implementation-integration` itself. The fixture has not been deleted. Read `docs/ops/evidence/2026-09-24-stage3-fixture-v1-recovery-disabled-preparation.md` and `docs/ops/stage-plans/2026-09-24-stage3-fixture-v1-identity-bound-cleanup.md`. Live cleanup and all later Stage 3 steps remain HOLD.

**Latest Stage 3 stop (24 September):** The one approved synthetic Keychain fixture ran once and returned `HOLD/CLEANUP`; the native journal stopped at `CREATE/COMPLETE/HOLD` before any read. macOS created the intended fixture file as mode `0644`, which the fixture's strict owner-only identity guard would reject. A zero-byte `.fl` sidecar also remains in the owner-only cache directory. The one-use journals are consumed; the local arming commit `c98822b` was reverted at `4e2b466`; no push or real credential read occurred. The default and user search-list Keychain stayed the login Keychain. The file and sidecar were **not deleted**. The disabled `fchmod`/anchored-directory repair passed independent Astra review and synthetic-file tests, including a directory replacement; full suite 2,468 passed, two skipped, with typecheck, manifest and boundaries clean. The final disabled fixture source SHA-256 is `7fb2673160069e91e9a6f07b0403f6a302eaf59627758d40dceadc6115445532`, binary SHA-256 `14f9a2c64ad10164e173390bba628b6c039d007f22d022c9bba58f90ca05352b`. Read `docs/ops/evidence/2026-09-24-stage3-fixture-v1-create-mode-incident.md` and `docs/ops/stage-plans/2026-09-24-stage3-fixture-create-mode-recovery.md`. **Next:** separately review and authorize identity-bound recovery of the existing fixture and sidecar, then prepare a new exact one-run plan and action-time approval. No successor run is authorized. Stage 3 provider/account/cart remains HOLD.

The checkpoints below this paragraph are historical; their old “next” directions were superseded by the incident above.

**Armed-build preparation (24 September):** A separate fixture-only armed-build/check script and launcher's gate-selected identity parser are prepared without arming or compiling an armed executable. With both live gates false, the armed builder's `--check` and `--build` return HOLD. The full suite passed 2,468 tests with two skipped; typecheck, targeted lint, activation manifest and live-boundary passed. Independent Astra review returned GO for this disabled work and HOLD for live execution. Read `docs/ops/evidence/2026-09-24-stage3-fixture-armed-build-disabled-preparation.md` and `docs/ops/stage-plans/2026-09-24-stage3-disposable-keychain-arming-design.md`. The remaining concrete work is to prepare and independently review the exact local two-gate-plus-manifest arming patch, refresh the local baseline, then seek separate action-time approval for a single synthetic fixture execution. Do not create or run a fixture yet.

**24 September disabled fixture checkpoint:** The disposable Keychain fixture source, private disabled build, one-use parent/native journals, offline tests and boundary pins are prepared. Independent Astra review returned GO for the disabled source and HOLD for live execution. The final suite passed 2,464 tests with two skipped; typecheck, targeted lint, activation manifest, live-boundary, both private disabled-build checks and diff checks passed. The fixture keychain has **not** been created or run; no real credential was read. The changed production reader source hash is `c1a4fcdbd63c4892a36f6da7f6b26231d666fa806eaf616d8b48873a23b276a7` and rebuilt disabled binary hash is `169065b2697a4b29680ae458e341ec93135f5cc825dfd71f0eb8a64ce08e87b1`. Fixture source hash is `a1ab8034afd8b26d44d6c5274e987212840ddd897caf7de378fdd64ac4f74614`; disabled fixture binary hash is `94ff3955517f1087c9666ba440989b797661ae3ca746461d2c0c91b9419b9dcf`. The old production disabled binary/receipt were archived with `.retired-<hash>` names, never overwritten or invoked. See `docs/ops/evidence/2026-09-24-stage3-disposable-keychain-disabled-preparation.md`. **Next coherent unit:** design the separate source-bound armed fixture build and exact one-run action plan, review it independently, refresh the local baseline and seek owner action-time approval before creating a disposable keychain. The current disabled build refuses an armed source. Incident and provider mutation remain OPEN/HOLD; Stage 3 account/cart activation and journey testing have not started.

**Latest disabled work unit (24 September):** The native legacy-login-Keychain reader has been written, compiled in a private disabled build, independently reviewed and verified offline. It is not connected to a launcher and has never accessed the real items. The full suite passed 2,447/2,449 tests (2 skipped), with typecheck, targeted lint, activation manifest, live-boundary, private build identity and diff checks passing. Read `docs/ops/evidence/2026-09-24-stage3-disabled-native-keychain-reader.md` and `docs/ops/stage-plans/2026-09-24-stage3-noninteractive-keychain-reader.md`. Source SHA is `46de62172b91f8258dd1ee1b109b984e1134c9c30416d83e6fb604e197b6daf4`; private disabled binary SHA is `92e4e3a57e51af29ce359bc19133e811f08857414c3e769c4b152fe1b0f5cad2`. A separate disposable-keychain qualification **proposal** is in `docs/ops/stage-plans/2026-09-24-stage3-disposable-keychain-qualification.md`; Astra approved it for disabled preparation only. **Next coherent unit:** implement its fixture-only source/build/driver with injected tests while disabled, independently review the diff, then produce an exact execution plan for owner action-time approval. Do not create a fixture keychain yet, invoke this binary against real items, change their ACLs, integrate a provider launcher or arm provider mutation. That later integration needs parent deadline and private-pipe failure controls. Incident gate remains OPEN/HOLD.

**Current HOLD (24 September):** The approved one-shot provider-normalization launcher was **not invoked**. Its exact Python Keychain reader failed in a precheck after one direct shell read had succeeded; the helper hid the underlying error at that time. Both mutation journals remain absent. The sibling normalization worktree was disarmed at `c08633e300ab22b8cc711c42d331bc0f7c1b1391`, with all nine gates false. A one-hour, project-scoped Vercel token was briefly saved in Keychain Comments and appeared in a local CUA output; Comments and clipboard were cleared, and the token was left to expire naturally. See `docs/ops/evidence/2026-09-24-stage3-provider-credential-storage-incident.md`. **Next coherent unit:** independently investigate why the earlier exact helper failed while the later bounded all-selector diagnostic passed; establish a supported, deterministic credential-access control or direct cause and review it under the incident-learning gate. Do not rerun the consumed readiness receipt, reuse historical arming commits, create a token merely to retry, or proceed to provider mutation based on one PASS. Once the incident gate is satisfied, refresh hosted state and review a new provider-mutation plan and exact diff before seeking separate action-time approval.

Diagnostic follow-up: the helper now distinguishes six fixed, secret-free failure categories. Its 7 offline isolated tests, manifest, live-boundary, focused session tests and typecheck passed; an independent reviewer returned GO for this repair only. Direct secret-suppressed reads later passed under normal and restricted environments, but did not explain the earlier helper failure. See `docs/ops/stage-plans/2026-09-24-stage3-credential-readiness-diagnostic.md`. The incident remains OPEN/HOLD.

The all-selector readiness process is now implemented **disabled** with a separate mode-0600 phase receipt and a 50-second absolute deadline; both new native gates are false after the completed one-run diagnostic. The injected offline tests cover ordered reads, deadline/slow-start failures, secret-buffer wiping, receipt replay refusal and native process parameters. Independent Astra review returned GO for the disabled implementation after deadline corrections. Full `npm test` passed before arming (2,444 pass, 2 skip, 0 fail); typecheck, activation manifest and live-boundary checks passed. A pre-existing hosted-baseline manifest hash was corrected to match the unchanged source file committed in `be38e13`; independent review returned GO for that single-hash correction. The incident remains OPEN/HOLD because the earlier intermittent Keychain failure is unexplained despite the bounded all-selector PASS.

Local preparation after commit `244f744`: a new, still-disarmed sibling checkout exists at `../implementation-provider-credential-readiness-arm-v1` on branch `codex/tll-provider-credential-readiness-arm-v1`, at that exact commit. All three fixed Keychain item metadata lookups returned present without requesting secret values. The shared `../implementation-state` parent was mode 0755 and would have blocked the new launcher; it was tightened to owner-only 0700 and verified, as was its `staging` child. Both provider-mutation receipts and the readiness receipt were absent. No token validity, ACL, hosted state or live Keychain read was proved. Do not infer readiness or arm from this metadata alone.

The local readiness action plan and incident wording were corrected after an independent execution review. A retained expired Vercel item was sufficient to test **local Keychain readability**, but could not prove API validity; this diagnostic used no new token or hosted baseline. The exact arming preview `/tmp/tll-credential-readiness-arming-preview-v1.patch` was applied once and later reversed in the isolated sibling: two source switches and their two activation-manifest hashes, three files total. The shared helper also serves the normalization and provider-readonly launchers, which remained disabled; direct helper invocation was prohibited because it outputs credential bytes. The consumed readiness receipt cannot be retried. The incident remains OPEN/HOLD despite the PASS.

The owner approved that one local diagnostic, and it returned terminal PASS from the exact helper. The private one-use receipt `../implementation-state/staging/tll-provider-credential-readiness-v1.json` is now consumed and must not be replayed. Both mutation receipts remain absent. The local readiness sibling was armed at `f5ef2d19a2cebaf62ddc2753ea507cf65fa9bda3`, run once, then disarmed at `086839dde2e2472edd72d82f8d6a1d0713cbd3a0`; it is clean and disabled, and no commit was pushed. See `docs/ops/evidence/2026-09-24-stage3-credential-readiness-v1-result.md`. This proves local readability only; the earlier intermittent failure and API validity remain unexplained, so Stage 3 provider mutation is still HOLD.

Independent incident review confirmed the earlier empty-password stdin provisioning and token-in-Comments error, but found no supported cause for the later exact-helper failure. The `/usr/bin/security` invocation and timeout did not change before the readiness PASS; exit-code classification was diagnostic, not a repair. The disabled native-reader source/build/injected-test part is now complete; its runtime behavior with a disposable non-secret item and any exact-item ACL change remain separate gates. Do not treat the implementation as incident closure. Apple distinguishes the file-based login keychain from the data-protection keychain; the reader targets the former and must be qualified on this Mac.

The owner-approved V2 official provider/database/secret preflight ran **once** from local-only arming commit `4e53ae4` and returned `READONLY_PROVIDER_PRECONDITION_VERIFIED` with strict database receipt hash `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`. The provider is still enabled with JWKS present, the broker secret name was absent and strict staging database invariants passed. The V2 private journal `../implementation-state/staging/tll-provider-readonly-probe-v2.json` is terminal `STOPPED_BEFORE_UPDATE` and must never be replayed. The sibling worktree was disarmed at `1b9e532`; its arming paths are byte-for-byte equal to disabled base `40b1654`, and native boundaries passed. See `docs/ops/evidence/2026-09-24-stage3-provider-readonly-refresh-v2-result.md`.

The eventual provider mutation remains limited to two fields (`enabled:false`, `jwks_uri:''`) and one update attempt, with in-session official full-field readback and separate categorical reconciliation. Customer/cart/Edge activation remains off until full Stage 3 journey acceptance.

24 September continuation: independent Astra review found and we repaired a Preview identity gap before arming: the normalization preflight now pins deployment `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b`, immutable URL and source `abd5a5258dd1072daf83a4447ce7110465f445e1`; its surface reader rejects changed identity before sending the retained bypass and checks alias again afterward. The disabled repair is `be38e13d34a1883aafe30619a631e066a1801086`. Focused tests 49/49, activation manifest, live-boundary, typecheck, targeted lint and diff checks passed. Independent rereview returned GO for the disabled repair. The exact nine-gate arming diff was independently reviewed and committed **locally only** in fixed sibling `../implementation-provider-normalization-arm-v1` at `ecfe6d1460b68d460b43601331f85e8225e953bd`; it was never invoked or pushed and was later disarmed. Dependency imports resolve and bundled Python hash matches. Last dashboards showed the provider enabled, three expected Edge custom secret names and the same Ready Preview source; those reads are now stale. See `docs/ops/stage-plans/2026-09-24-stage3-provider-normalization-one-shot-gate.md`.

After Stage 3, the release plan still requires verified unified account/cart/orders/logout, inventory and order workers, supplier commercial/VAT basis, retail pricing and flash sales, affiliate tracking/discount/refund accounting, consent/email operations, research-backed scores, theme/customer journey consistency, backup/restore and production release checks. The offline pricing/affiliate modules are not connected to live checkout or payouts. Product-level score verification awaits source labels/Path A evidence.
