# Agent handover — The Lifting Lab integration

Updated: 2026-09-23 after disabled Stage 3 provider launcher review. Verify this against Git and hosted state before acting.

## Objective and boundary

Continue the staged comparison-site/Shopify integration under `docs/ops/project-stage-execution-protocol.md` and `docs/ops/integration-release-candidate.md`. This email-capture work changed only the live Shopify password-page copy, double-opt-in setting and authorized DNS records. A single confirmation email was sent through the owner's test signup. No purchase, supplier order, promotional customer message, provider enablement or deployment occurred. Public launch remains held.

## Current state

- A pure disabled Stage 3 Preview deployment request contract now pins the team/project, staging branch, full Git commit, activation-manifest hash and GitHub repository ID `1264363509` (from read-only GitHub repo API). It has no transport, token or launcher, rejects enabled public flags and cannot select Production. The native `createDeployment` still throws and the activation manifest still says deployment creation is unimplemented. The first full suite found an omitted fixed source-list expectation; that was corrected, and full disabled tests passed 2,318/2,318. Focused 18/18, both manifest checks, live-boundary, typecheck and lint (0 errors, 20 existing warnings) passed. Vercel's **connected** repository ID and actual Git/Preview request shape remain to be independently verified before adding a POST binding. See `docs/ops/stage-plans/2026-09-23-stage3-preview-deployment-request.md`. No hosted mutation occurred.
- Stage 3 readiness was rechecked without opening a credential window or invoking a launcher. V8 journal hash/mode remain unchanged, all native gates are false, focused hosted tests passed 47/47, both manifests `--check` and the live-boundary check passed. The exact staging Supabase dashboard still showed the custom subject-broker provider ENABLED with a JWKS URI. Vercel's current `codex/tll-integration` Preview was Ready at deployment `4M6P7P6rbG9w2Q9kC6y65rRzWB3V` but source `536d36a`, behind local HEAD. The UI could not prove custom `meta.tllManifestSha256`, and source inspection found no writer for it. A new observer now would repeat known HOLDs without advancing sign-in acceptance. See `docs/ops/stage-plans/2026-09-23-stage3-readiness-reconciliation.md` for exact evidence and the next design gate. No hosted settings changed.
- Prelaunch shop email capture is live on the password page (published theme `207410200906`): clear launch/promotions consent copy, “Join the launch list” button and confirmation-aware success text. Marketing double opt-in is enabled for new email subscribers only. One owner-controlled signup moved from Pending to Email Subscribed after the confirmation email link; no purchase or customer campaign occurred. Local theme commit is `48c35ee`. The owner approved and we added Shopify's six exact authentication CNAMEs plus `_dmarc` TXT in Netlify; each resolved correctly at its authoritative nameserver, with Microsoft 365 MX and existing SPF intact. After one Shopify recheck, Admin reported **Propagating**, not yet Authenticated. See `docs/ops/stage-plans/2026-09-23-prelaunch-email-capture.md` for DNS values, evidence, drafts and consent boundaries. Next read-only check: Shopify must show Authenticated before sender testing or campaign activation. The comparison-site privacy page does not yet cover this marketing list; existing legacy Shopify subscribed contacts need consent review before ongoing offers. Shopify Flow is not installed and no welcome automation or campaign is active.

- Repo `implementation-integration`, branch `codex/tll-integration`. Verify `pwd`, branch, HEAD, status and upstream. The v7 observer was armed in `898bf0d` and disarmed in `53ffb67`; later checkpoint commits may advance HEAD. Both `HOSTED_BASELINE_LIVE_ENABLED` and `APPROVED_NATIVE_READ` are false; generated manifest declares `nativeAccessApproved:false`. The v7 journal path is `../implementation-state/staging/tll-hosted-baseline-observation-v7.json` and is consumed.
- Preserve the unrelated untracked `.agent/gen11-journal-watch.mjs`, `implementation-state/` and iCloud ` 2` copies. Never stage by wildcard. The v4 full observer journal and v2/v3/v4 database journals are consumed. The v5 database journal is terminal PASS with hash `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`.
- The v5 **full** observer ran once and returned `RECONCILIATION_REQUIRED`. Its separate mode-0600 journal `../implementation-state/staging/tll-hosted-baseline-observation-v5.json` is consumed at `INTENT_RECORDED`, run ID `11c13cc4-d008-461c-9363-c9179545a08b`, SHA-256 `3eaec61fdc96cabd9c2123700800fb267d2f4cfc37101d3175e6d82e2a659ecd`. Do not replay or alter it. Separate read-only HTTP 200 checks confirmed the exact Supabase staging and Vercel project identities; no primary failed request was identified.
- The direct local issue is an over-broad cleanup classifier: a native errored stream can reject both `read()` and `cancel()` after all work has settled, leaving the intent journal open and masking the first fixed read reason. `4709987` corrects it by accepting only pre-release `reader.closed` rejection as terminal error proof, retaining arbitrary failed cancellation as uncertain, and returning a fixed `diagnosticReasonCode` when cleanup remains uncertain. Independent review found and resolved a native underlying-cancel counterexample. Focused 32/32 and full disabled 2,254/2,254 passed; independent review GO. See `docs/ops/stage-plans/2026-09-22-v6-observer-cleanup-classification.md` and `2026-09-22-full-hosted-observer-v5.md`.
- The v6 full observer ran once and returned terminal `OBSERVATION_FAILED`, reason `vercel_environment_read_unavailable`. Its mode-0600 journal has run ID `a3286073-5169-4f27-b59b-01efd2c32d52`, SHA-256 `faaf41b67a1d8f8ba1b9f42c744f3c77052c15f6819878b8c04e737af3706d54`, and no observation hash. Never replay or alter it. Separate read-only inspection of the exact Vercel Preview environment URL returned HTTP 200, 16 branch-specific Preview entries, and no `pagination` object. The direct cause was a parser rule that wrongly required a terminal pagination cursor for a short page. A disabled correction accepts absent pagination only below the fixed 100-entry request limit; full pages without a cursor and malformed/non-terminal cursors still fail closed. Focused 11/11 and full disabled 2,254/2,254 tests passed; independent review GO. See `docs/ops/stage-plans/2026-09-22-full-hosted-observer-v6.md`.
- The v7 disabled package, provider parser and exact arming diff all received independent review GO. Focused 46/46 and full 2,255/2,255 tests passed while disabled. The one-shot v7 run returned terminal `OBSERVATION_FAILED`, reason `observation_validation_unavailable`; its mode-0600 journal has run ID `c66a7537-a63b-4071-baab-cd3442516ad2`, SHA-256 `d53760204f68ca4949af4be340f2dadaf4e3442abe24b91a6eb66e5f6c4d6084`, no observation hash. Never replay or alter it. Separate read-only serial reconciliation found database PASS, all individual provider/secret/Vercel/surface reads successful, matching project/deployment repository identity, and a locally assembled HOLD receipt that passed pure projection. The direct cause of the concurrent composite generic validation failure remains unresolved; the owner's later one-time v8 exception superseded this historical stop decision. See `docs/ops/stage-plans/2026-09-22-full-hosted-observer-v7.md`.
- A disabled diagnostic reproduced generic error masking with malformed provider data and found a second masking path for repository mismatch during concurrent settling. Independent review found and prompted corrections for malformed successful secret-inventory and shared-surface envelopes, then returned GO on the corrected disabled diff. Core, composition and session now propagate finite fixed stage labels with constant error messages; tests cover these counterexamples. Focused 29/29 and full 2,256/2,256 pass; both manifest checks and live-boundary checks pass. This improves attribution but does **not** establish which stage failed during v7. See `docs/ops/stage-plans/2026-09-22-v7-validation-diagnostic.md`.
- A deterministic offline concurrent replay released five staging-shaped reads in forward, reverse and surface-first orders. Every order returned the same three-reason HOLD/hash with one shared surface read and one disposal per binding. Focused 10/10 and full disabled 2,257/2,257 pass; independent review GO. This narrows the order-of-completion hypothesis for these fixtures, but v7's live trigger remains unknown. See `docs/ops/stage-plans/2026-09-22-v7-concurrent-replay.md`.
- The reviewed incident decision `docs/ops/stage-plans/2026-09-22-v7-incident-decision.md` records that the direct cause remains unknown. Nine targeted retained `/private/tmp/tll-v7*.log` files contain neither the v7 run ID nor its failure code; that does not exclude evidence elsewhere.
- On 2026-09-23 the owner approved **preparing** a one-time v8 diagnostic exception, then separately approved its one-run credential window. The disabled package was reviewed before the later live action. See `docs/ops/stage-plans/2026-09-23-v8-diagnostic-observer-preparation.md`.
- The owner then required a cross-attempt failure map before any further hosted run. `docs/ops/stage-plans/2026-09-23-v8-failure-prevention.md` records v4-v7 causes/limits and five pre-run stop gates. A deterministic offline test exposed that the tracker could report settlement before late response cancellation finished. The disabled correction adds one quiescent event-loop turn and a separate five-second cleanup grace; timeout wipes session-owned credentials and leaves the journal at intent with `RECONCILIATION_REQUIRED`, never a false terminal receipt. Focused session 24/24 and full disabled 2,261/2,261 pass, both manifest checks and live-boundary check pass; independent review GO. V7 hash unchanged; v8 still absent.
- Fresh preflight passed the strict staging database receipt, provider schema, secret-name absence, Vercel project/GitHub identity, exact Preview environment envelope (HTTP 200, 16 matching rows, no pagination), deployment commit and disabled surface flags. A first Keychain item had reversed labels; exact readback caught it before any hosted request. Editing its ACL stalled, so the local item was removed. A disposable interactive CLI rehearsal with `-T /usr/bin/security` passed; that method created the correct one-hour token item without exposing it in command output. All three exact helper reads passed. The independently reviewed arming diff was committed as `cc4477b` and invoked **once**. V8 returned terminal `OBSERVATION_FAILED`, reason `vercel_merge_unavailable`; journal run ID `68bfe246-3308-4690-854d-845245bca34b`, mode 0600, one link, SHA-256 `49ac5c4e6fe22fb384a363a24dbc46b308a37b81e28503fbb8767ec8a10b83fd`, no observation hash. It is consumed: never replay or alter it. Disarm commit `ccb0209` restored both false gates and manifest; live-boundary check passed.
- Separate read-only diagnosis found the direct v8 cause: Vercel project `link.sourceless` was `true`, while the project/deployment GitHub repository IDs, provider, branch and commit matched. The composition code demanded literal `false`, unlike its Vercel parser's boolean contract. Separate Supabase reconciliation still passed with 15 migrations, zero enabled controls, five runtime roles, zero sessions/edges, absent broker secret and the known enabled/JWKS provider. A red test reproduced the fixed merge code; the one-line disabled correction now accepts either boolean only when repository/provider identity matches. Negative tests still reject mismatches and malformed data. Focused 40/40, full disabled 2,263/2,263, both manifests, live-boundary and lint pass (0 errors, 20 existing warnings); independent review GO. See `docs/ops/stage-plans/2026-09-23-v8-incident-review-and-disabled-correction.md`.
- The local v8 Vercel API Keychain item was removed; its remote one-hour token is left to expire naturally. The retained non-expiring Preview bypass remains with its temporary `/usr/bin/security` allowlist entry removed and “Confirm before allowing access” selected. No credential window is open.
- While Stage 3 remains held, an offline Stage 6 slice added `lib/commerce/marketing-contract.ts` and `tests/marketing-contract.test.mjs` under `docs/ops/stage-plans/2026-09-23-stage6-offline-commercial-contracts.md`. It validates draft retail, campaign, affiliate, attribution and ledger record shapes and dependencies but **always** returns `eligible:false`, `liveEnabled:false`, `payoutExecutionAuthorized:false` and zero satisfied manual payout reviews. A complete-looking record is not an approved price, commission, payout or code. An independent review found false-eligibility cases in the initial design; the design was narrowed, malformed-ID error paths corrected, and the final offline boundary received GO. The activation manifest was regenerated because it recursively pins `lib/commerce`; `--check` and the full disabled suite passed 2,275/2,275. Typecheck and lint passed (20 existing warnings, no errors). No Shopify or database write occurred.
- An offline scoring evidence inventory at `docs/research/scoring-evidence-inventory.md` lists all 15 methodology groups, guide-citation coverage and the missing product-level approval evidence. Independent review prompted explicit `liver-health` and vitamins/wellbeing category coverage corrections. Historical scores remain contained; no formula, score or public recommendation changed.
- The next Stage 6 offline unit added `lib/commerce/commercial-burden.ts` and focused tests under `docs/ops/stage-plans/2026-09-23-stage6-shadow-commercial-burden.md`. It reuses `evaluateBasket` and subtracts four explicit per-line affiliate/campaign/sponsorship/platform costs from exact conservative line and order contributions. Missing or duplicate allocations HOLD; each line and the order must meet the existing minimum margin and cash guards. Mathematical PASS is distinct from authority: `eligible`, live, checkout and Shopify-allocation flags always remain false. Independent review ran boundary and malformed-input probes, requested durable regressions, then returned GO. Focused 9/9 and full disabled 2,284/2,284 passed; typecheck/lint and both manifest checks passed. The activation manifest pins the added source. No provider or Shopify action occurred.
- The offline daily competitor price-check foundation added `lib/commerce/competitor-price-check.ts`, `docs/ops/daily-price-checks.md` and focused tests under `docs/ops/stage-plans/2026-09-23-daily-competitor-price-check-foundation.md`. It compares delivered prices only for fresh, in-stock, reviewed exact variant/pack matches under one shipping scenario; records every supplied offer as comparable or held; reports floor conflicts without recommending a Shopify price. Independent review found and corrected stale snapshot, match expiry, sibling offer, sparse-array and accounting/identity issues, then returned GO. Focused 10/10, full disabled 2,294/2,294, typecheck, lint (0 errors, 20 existing warnings), both manifests and live-boundary checks pass. No data source, scheduler or price write exists yet. The planner cannot prove a supplied snapshot covers the entire Shopify catalogue.
- The next offline unit adds `lib/commerce/price-check-catalogue-receipt.ts` and synthetic tests under `docs/ops/stage-plans/2026-09-23-price-check-catalogue-receipt.md`. It HOLDs missing/extra variant IDs, broken or repeated page cursors, duplicate IDs, wrong shop/scope/snapshot bindings and count mismatches. A reviewer found a terminal cursor reuse false PASS; the correction and regression received GO. Focused 6/6 and full disabled 2,300/2,300, typecheck, both manifest checks and live-boundary check pass; lint has zero errors and 20 existing warnings. This checks only internal consistency of caller-supplied evidence. It does not authenticate Shopify, independently verify the declared count, prove a stable paginated snapshot, or bind actual prices to page IDs. It is read-only with automation and price authority disabled. No hosted access or live action occurred.
- The offline Shopify variant read package at `lib/commerce/shopify-variant-snapshot.ts` has fixed unfiltered 2026-07 Admin count-before/page/count-after documents and an injected request port, with no default transport or credential lookup. It requires expected shop ID, GBP, `EXACT` equal counts, strict product/variant identity, calendar-valid timestamps, bounded pence and a passing page receipt; it returns no partial snapshot on HOLD. A reviewer found invalid-date and caller-scope false PASS paths; strict validation and a fixed all-variants scope corrected both, with regression tests and final GO. Focused 16/16, full disabled 2,316/2,316, typecheck, both manifests and live-boundary checks pass; lint has zero errors and 20 existing warnings. `shadowUseAuthorized`, automation and price authority remain false. No Shopify API request, deployment or live change occurred. See `docs/ops/stage-plans/2026-09-23-shopify-variant-read-package.md`.

## Next coherent unit

Stage 3 remains HOLD for hosted arming. The bounded injected provider-normalization session is committed at `eac7cd3`; its exact offline source had independent GO. The next disabled launcher at `scripts/staging-provider-normalization-live-launcher.mjs` now supplies fixed staging-only Supabase/Vercel/surface readers, the official provider port, separate phase/intent journals and a dedicated disabled Keychain helper. Initial independent review found that a shared helper conflicted with the consumed hosted observer manifest and that the live-boundary checker missed the new gate. Both were corrected; re-review gave GO for a disabled commit and HOLD for hosted arming. The launcher returns `PROVIDER_NORMALIZATION_LIVE_DISABLED` before credentials or hosted calls. Focused 22/22 and full disabled 2,357/2,357 passed; typecheck, both manifest checks, live-boundary and diff checks passed; lint had zero errors and 20 existing warnings. See `docs/ops/stage-plans/2026-09-23-stage3-provider-live-launcher-disabled.md`.

Next coherent unit: obtain a **fresh, read-only staging baseline** for exact Supabase provider/Admin fields, frozen database/secret/flag state, and Vercel Preview identity/manifest proof. Historical observations show the provider enabled and Preview behind local HEAD, so do not presume readiness. Then draft the minimal provider-launcher/helper arming diff, independently review that exact diff, obtain action-time credential/permission authorization, run one maximum bounded staging attempt only if all gates pass, reconcile separately and disarm. Do not replay V8, create an ad hoc observer, edit the Supabase dashboard, touch Production, push/deploy this branch or buy anything. No credential window is presently approved. Preserve the unrelated untracked files and consumed journals.

The read-only Vercel Git settings check on 23 September confirmed `tntipper/the-lifting-lab` is connected to this exact project, but did not expose numeric `link.repoId`. The provider repair design and disabled pure contract are now in `docs/ops/stage-plans/2026-09-23-stage3-provider-normalization.md` and `scripts/staging-provider-normalization-contract.mjs`: disable the existing staging provider and clear only its JWKS URI, with complete official-response readback, independent review and a one-shot journal. The installed Supabase SDK declares both fields optional for a partial update. Focused 11/11 and full disabled 2,321/2,321 passed, along with typecheck, both manifests and live-boundary; lint 0 errors, 20 existing warnings. The first full suite found a stale manifest after the final source change; regeneration and repeat passed. This is **not armed**, has no live transport or independent review, and no provider setting changed. Do not use the Supabase dashboard for an ad hoc edit or run another baseline observer while these known HOLDs persist.

Final read-only Supabase UI preflight revealed blank scopes and display name `tll-staging-subject-broker-v1` in addition to enabled/JWKS. The form was closed without saving. The disabled normalization contract was corrected and re-pinned to accept only the observed/desired name and scope values while preserving them exactly; the later broker rotation sets its final desired state. Focused 11/11 and full disabled 2,321/2,321 passed again, with typecheck, manifests and live-boundary passing. A fresh official Admin API read must still confirm hidden fields/PKCE before any one-shot normalization; the UI alone is insufficient. Independent review and arming remain outstanding.

The next disabled Stage 3 unit adds `scripts/staging-provider-normalization-native-port.mjs` under `docs/ops/stage-plans/2026-09-23-stage3-provider-normalization-native-port.md`. It reuses the fixed official SDK client through injected project material, executor and fetcher, derives only the two-field update and requires full SDK-response comparison; a separate GET remains necessary. No launcher, credential window, hosted request or provider edit occurred. Focused 14/14, full disabled 2,324/2,324, typecheck, both manifest checks, live-boundary and diff checks passed; lint 0 errors, 20 existing warnings. The pure contract and native port need **independent review**. Then prepare a separate durable one-shot journal/orchestrator and fresh Admin/database/secret preflight before any arming. Preserve all unrelated untracked files and consumed journals.

Before review, the native port gained a per-instance one-update guard; even an uncertain result consumes that instance. Re-pinned final source passed focused 14/14 and full disabled 2,324/2,324 again, with typecheck/manifests/live-boundary passing and lint unchanged. The owner explicitly approved a separate delegated reviewer for this disabled repair code on 23 September. A durable cross-process journal is still required before any hosted update.

The independent reviewer returned GO for disabled journal preparation at `caebad4`, HOLD for hosted arming, with no actionable finding; see `docs/ops/stage-plans/2026-09-23-stage3-provider-normalization-journal.md`. A distinct mode-0600 exclusive intent/phase journal is now prepared, with ordered terminal transitions and cross-instance replay rejection. Focused 11/11 and full disabled 2,327/2,327 passed; typecheck, both manifest checks, live-boundary and diff checks passed; lint 0 errors, 20 existing warnings. It has no coordinator, live launcher, credential path or hosted effect. Next independently review this journal, then build the disabled coordinator with fresh database/flag/secret/provider preflight and post-read. Do not arm or mutate staging on the journal alone.

Journal review of `848ac2f` returned HOLD: both write paths ignored possible short writes. The direct cause was copying a single-write pattern without fault-injection tests. No hosted action occurred. A checked write-all loop and real-file partial/zero/throw regressions now protect creation and transition; focused 13/13 and full disabled 2,329/2,329 passed with typecheck, both manifests, live-boundary and diff checks; lint 0 errors, 20 existing warnings. The correction was then independently re-reviewed as described below.

Independent re-review of corrected journal `d8a1540` returned GO for a disabled coordinator, HOLD for hosted arming. The coordinator is now prepared at `scripts/staging-provider-normalization-coordinator.mjs`: exact fresh frozen-state receipt, immutable provider/preflight snapshots, durable intent before one update, separate provider GET and later frozen-state postflight, fixed reconciliation on post-intent uncertainty. It has no native preflight adapter or live launcher and all enable flags remain false. Focused 16/16 and full disabled 2,332/2,332 passed; typecheck, both manifests, live-boundary and diff checks passed; lint 0 errors, 20 existing warnings. Next obtain independent review of this exact coordinator, then implement authoritative staging-only read ports and separately reviewed live-window mechanics. No hosted settings changed.

Coordinator review found an expiry gap: a slow provider read or journal persistence could outlive the 30-second preflight. The correction revalidates the captured baseline after the provider read, just before intent and again after durable intent before dispatch. Delayed-read and delayed-journal regressions passed, and independent re-review returned GO for planning disabled native preflight ports; hosted arming remains HOLD. Focused 18/18, full disabled 2,334/2,334, typecheck, activation manifest and live-boundary checks passed. Next implement the authoritative staging-only preflight adapter using the fixed read-only database receipt plus individual secret/surface reads, with fresh timestamps, identity and negative-boundary tests. Do not reuse the composite observer that already has known HOLDs; no credential window is open.

The disabled preflight adapter is now in `scripts/staging-provider-normalization-preflight.mjs` with a plan at `docs/ops/stage-plans/2026-09-23-stage3-provider-preflight-adapter.md`. It composes fresh fixed read bindings, proves the retired database receipt, broker-secret absence, Vercel repository/Preview identity, disabled Edge and all four customer/cart flags, and stamps observation from the read's start so slow reads fail. Provider reads use a separate fresh Supabase binding. Review found and corrected an interface mismatch: the real Vercel binding has no `target` property; target is on its returned receipts. A test now constructs that real binding with synthetic HTTP; cleanup failures also fail closed. Independent re-review returned GO for the next disabled integration/launcher-planning step, **HOLD for hosted arming**. Focused adapter/manifest 13/13, full disabled 2,339/2,339, typecheck, manifest and live-boundary checks passed; lint 0 errors, 20 existing warnings. Next design the bounded native launcher with concrete binding factories and an explicit deadline signal, then review the full read/update boundary before any action-time credential window. The adapter's injected fixtures alone do not prove current hosted state. No hosted action occurred.

The next disabled unit began under `docs/ops/stage-plans/2026-09-23-stage3-provider-launcher.md`. The older generic phase journal ignored short writes, so a dedicated provider-normalization phase journal was built instead, with exclusive mode-0600 creation, checked writes/fsync, ordered phases/deadlines, and consumed-file replay rejection. Independent review found a stale-phase reset defect; transition-time deadline checks and real-file regressions corrected it. Re-review returned GO for **disabled launcher preparation**, HOLD for hosted arming. Focused phase/manifest 14/14 and full disabled 2,346/2,346 passed, with typecheck, both manifest checks and live-boundary; lint 0 errors and 20 existing warnings. No hosted action or credential read. Next wire a disabled bounded launcher with the reviewed concrete readers, explicit abort signal, private project-secret retrieval, the phase journal and the separate provider intent journal. Review the entire read/update boundary independently before any credential window. Preserve existing untracked files and consumed journals.

The fixed staging Supabase read binding now exposes `readProjectSecret({signal})` for the future one-run launcher. It reuses its bounded Management API key endpoint and exactly-one legacy service-role selector. First review found that an in-flight read could return after `dispose()`; post-await disposal/abort checks now wipe/reject late key delivery, with deferred-fetch/body regressions. Re-review returned GO for disabled launcher wiring, HOLD for hosted arming. Focused 19/19 and full disabled 2,348/2,348 pass; typecheck, both manifests and live-boundary pass; lint 0 errors, 20 existing warnings. Plan: `docs/ops/stage-plans/2026-09-23-stage3-provider-secret-read.md`. The future launcher must wipe a successfully returned key Buffer in `finally`. No hosted access or credential window occurred.

For Stage 6, next plan an authoritative price/campaign approval source and a content-bound affiliate ledger/reconciliation design. The shadow burden calculation can support synthetic planning but not publication. Obtain the outstanding wholesale VAT basis, payment tariff, Shopify discount-allocation evidence and affiliate terms before any live calculation. Do not connect the offline Stage 6 modules to checkout, Shopify discounts or payouts. For daily price checks, next define and approve a lawful competitor feed, full Shopify snapshot/pagination receipt, shipping scenarios, match-review registry and freshness policy; then run the complete catalogue in shadow mode before any schedule or price action. For scoring, acquire original Path A inputs or fresh manufacturer labels and review the product-level evidence queue, beginning with cycle/liver, hormone/ZMA and gut/digestion; the recommendation hold stays in place until explicit independent approval.
The offline catalogue receipt covers supplied page/count consistency, and the injected-port Shopify read package now exists, but no authenticated transport is connected. Next plan a staging-only or otherwise approved read-only Admin adapter, verify `read_products` access and exact shop identity, capture source receipts without exposing credentials, and reconcile default-shop prices against UK customer-context prices and shipping before any shadow run. Matching exact counts cannot prove a stable transactional snapshot; retain this limit. Lawful competitor sources, shipping scenarios, reviewed matches and freshness remain prerequisites. Stage 3 hosted account sign-in remains on HOLD separately.

The broader backlog remains in `docs/ops/integration-release-candidate.md`: unified account/cart/orders/logout, inventory and order workers, supplier-cost/VAT basis, retail pricing/flash sales, affiliate attribution/discount/refunds, backup/restore, consent/email, scientific scoring review, theme publication and production release checks.
