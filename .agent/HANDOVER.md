# Agent handover — The Lifting Lab integration

Updated: 2026-09-24. Verify every claim against Git and hosted systems before acting.

## Latest checkpoint — staging source published, runtime gate held

This section supersedes the older pre-publication snapshot below. The approved single staging push **has occurred**: remote `codex/tll-integration` and protected Ready Vercel Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` were observed at immutable source `abd5a5258dd1072daf83a4447ce7110465f445e1`. Source proof returned `SOURCE_PROOF_VERIFIED`, manifest `4f05bca4a90983ea8b5e72d23f5c5fe4e69110dfbb90ccde1ff1d53dcde6e5c3`. See `docs/ops/evidence/2026-09-24-stage3-staging-preview-publication.md`. Local-only arming commit `e8a68325b7d87b7878d95b88f984d1d7743f1683` was never pushed and is now disarmed at local commit `609d5e4`. Preserve the consumed publication journal and all six pre-existing unrelated untracked paths. Do not publish again to obtain runtime evidence.

The account/cart runtime remains **unverified and held**. The direct signed-in browser request to `/api/staging/readiness` was blocked by the browser client. A protected read-only runtime proof was independently reviewed, but its V1 journal was consumed by a faulty offline test before Keychain or network access; see `docs/ops/evidence/2026-09-24-preview-runtime-proof-v1-offline-harness-incident.md`. V1 is terminal HOLD and must never be retried or deleted. A V2 script/plan and private-copy offline harness are in sibling private `implementation-state/staging/`; the exact V2 harness passed expiry-before-Keychain and replay checks, and an independent reviewer returned GO **to request approval only**. V2 has not run; its journal was absent at the last check. A fresh owner question about the V2 credential window is pending. Do not construe the earlier V1 approval as V2 approval. If the owner approves V2, recheck the exact source/deployment/alias/protection, V2 journal absence and script, then execute the plan's fixed isolated command **once**; stop on any HOLD or uncertain result. No activation, purchase, customer send or production change is included.

The source branch may now have local evidence/handover commits newer than deployed S. This does not change the immutable Ready Preview at S. A future Preview would require its own source selection and stage gate. After V2, record the secret-free terminal receipt, independently reconcile any uncertainty, and keep customer/cart/Edge activation off until the full Stage 3 acceptance gate passes. The broader release backlog below remains open.

## Objective and boundary

Deliver the staged comparison-site/Shopify integration in `docs/ops/integration-release-candidate.md` under `docs/ops/project-stage-execution-protocol.md`. The immediate gate is Stage 3 staging account/cart acceptance, followed by the remaining inventory/order, pricing/affiliate, scoring, design and public-release work. No purchase, supplier order, promotional customer send, production provider change or public launch has been authorized. Connected-branch pushes and hosted credential windows need a reviewed exact plan and fresh action-time approval. Keep customer/cart/Edge activation held until all evidence passes.

## Current state

- Repository: `implementation-integration`, branch `codex/tll-integration`. Check current HEAD and status; the committed base before this disabled-launcher unit was `4c8b84b`. The remote branch was last observed at `0f8027288ac52667089a094b73a2c03ff63ca916`, behind local source. There has been no push or deployment in this checkpoint.
- Current unit: `scripts/staging-preview-git-acceptance.mjs` is a pure, disabled comparator. It requires the selected full SHA and Git manifest digest, matching before/after Vercel source proof, fixed project/team/repository/branch/Preview identity, one matching deployment-bound surface read, and all four customer/cart plus Edge controls disabled. It returns only `SOURCE_PROVEN_RUNTIME_HELD` or fixed HOLD. It does not activate sign-in. See `docs/ops/stage-plans/2026-09-24-stage3-git-preview-source-proof.md` and tests. Independent review: GO for this disabled unit, HOLD for hosted integration. The consumed v8 hosted-baseline journal/launcher/core were not changed or replayed.
- Fixed read-runner recovery: the source-proof CLI now uses a pinned native Git binary on this Mac, with exact binary/helper digests, helper link/directory checks, and the existing fixed command/remote allowlist and scrubbed environment. Initial review caught a missing HTTPS helper path; an offline helper-dispatch regression exposed and then verified its correction. Independent re-review returned GO for read-only recovery, HOLD for publication. No push command was added. See the fixed-executable section of the source-proof plan.
- The fixed read-only CLI returned `SOURCE_NOT_AT_REMOTE` from a clean tree at `a53c5ee`, confirming transport worked and the remote branch still lagged. A distinct `staging-preview-git-publish-journal.mjs` now records the selected full SHA, predecessor and manifest digest in an exclusive private one-use file and requires durable dispatch before any future push. First review found valid-record rollback could authorize a second dispatch; retained in-memory state and transition-attempt latches corrected it. Re-review GO for disabled journal, HOLD for runner/hosted publication. No real publication journal or push was created.
- `staging-preview-git-publish-coordinator.mjs` now provides an injected-only, single-use publication sequence. It reads the remote predecessor, records durable intent/dispatch in the reviewed journal, calls one injected push port, and reconciles the remote once even on uncertain acknowledgement. It yields only a source-published/Preview-unverified receipt or HOLD. Independent review returned GO for this disabled unit, HOLD for native binding/hosted publication. No native push port, real journal or push was created. See the publication-coordinator section of the source-proof plan.
- `staging-preview-git-publish-native-contract.mjs` now selects source through injected Git results with explicit zero exit status, validates the effective local/worktree config allowlist, proves predecessor ancestry and committed manifest digest, and describes one fixed lease/refspec push command. It has **no process adapter or push capability**. Review found and resolved a non-ancestor status gap; final independent review GO for pure disabled contract, HOLD for native integration. See the disabled-native-contract section of the source-proof plan.
- `staging-preview-git-publish-native-read.mjs` now provides a fixed **read-only** process port. Its first review found a caller-array method spoof that could validate a safe command and spawn a different one; the correction captures a frozen primitive argument snapshot and uses it for both policy and spawn. Re-review GO for read-only port; real local config and both merge-base statuses passed. No push adapter exists.
- At clean HEAD `10ecd36`, the fixed native selector returned `PUBLISH_SOURCE_SELECTED`: source `10ecd36f8a2aa8b85341b2e1be4021afc3b97aa4`, predecessor `0f8027288ac52667089a094b73a2c03ff63ca916`, manifest digest `2dd90a21b03d756b78eb29b93fc22ec89ec677f9a0f6f82b15636b9889c35283`. This was one read-only observation and is stale after any new commit.
- The injected coordinator now requires an exact fresh `recheckSource` selector receipt after remote pre-read and before journal dispatch. Mismatch, missing proof, accessors or thrown read HOLD without journal/push. Independent review GO for this disabled correction only; the future native binding must supply the fixed selector/read port. No push transport or real journal exists.
- `staging-preview-git-publish-binding.mjs` now composes selector, strict remote decoder, coordinator and journal through injected ports only. It requires the exact action-approved source/predecessor/manifest tuple before any read, reselects before dispatch, then reconciles one injected push once. Review found and corrected a missing approved-source pin and remote decoder test gaps; independent re-review GO for this disabled composition. No live journal or publication exists.
- The push contract now describes command-local GitHub CLI credential access for the exact staging HTTPS URL/refspec, fixed expected-value lease, ignored output and a scrubbed Git environment. `staging-preview-git-publish-native-push.mjs` is **injected-only**; it imports no process adapter. Initial review found a live `spawnSync` default that bypassed the future launcher; this was removed and re-review GO. The pinned GitHub CLI binary/config metadata check reads no credential. No real push/helper/journal was invoked. See the native-transport section of the stage plan.
- The clean-source versus tracked-arming conflict has an approved design: keep S clean in `implementation-integration`, create a separate local-only arming worktree at A, and push only S. `staging-preview-git-publish-arm-preflight.mjs` is injected/read-only and requires fixed worktree/branch, approved A directly descended from S, exact two changed paths, regular committed blobs, and local bytes matching those blobs. Its default reader is bounded with `O_NOFOLLOW`. Independent review GO for this proof.
- The fixed native arming read port now binds that proof to a pinned local Git binary and fixed sibling cwd. Independent review found a partial-clone lazy-fetch path; `GIT_NO_LAZY_FETCH=1` and a fixed protocol deny close it, with a local promised-blob/helper-dispatch regression. The same no-lazy guard was applied to the earlier Git source-proof readers. Re-review GO for read-only scope. No arming worktree exists yet.
- The **disabled** staging Git publication launcher now exists at `scripts/staging-preview-git-publish-live-launcher.mjs`. It fixes S and sibling A, begins with a literal false gate, verifies source and A proofs, and binds the real push process only after those gates to the one-use journal. Initial independent review found that Git assume-unchanged can conceal modified executable files; the corrected launcher compares local source manifest/checker/import-closure and A checker bytes to committed S blobs before executing repository code. Re-review GO for disabled scope only. No A worktree, journal, credential read, push or deployment occurred.
- Previous live staging observation: the protected Preview alias resolved to Ready deployment `dpl_4M6P7P6rbG9w2Q9kC6y65rRzWB3V` at old source `536d36a515ad39449338748b5739b0dcdedf8256`, with no custom manifest metadata. Repository ID matched `1264363509`, but current source was unproven. See `docs/ops/evidence/2026-09-23-preview-source-read-result.md`. The one-use journal was consumed; native gates were disarmed. Re-observe before any external action.
- Staging Supabase read-only probe established the provider precondition and retired database/secret state, but provider normalization and account activation remain held. See `docs/ops/evidence/2026-09-23-stage3-provider-readonly-probe-result.md` and the Stage 3 plans. No live account/cart acceptance has passed.
- Six unrelated untracked paths are present and must be preserved: `.agent/HANDOVER 2.md`, `.agent/gen11-journal-watch.mjs`, `config/staging-account-hosted-baseline-manifest 2.json`, `implementation-state/`, `scripts/staging-account-hosted-baseline-keychain 2.py`, `scripts/staging-account-hosted-baseline-live-launcher 2.mjs`.

## Decisions and validation

- Use a normal Git-triggered Vercel Preview plus independent immutable Git source/manifest proof and deployment-bound runtime readback. Do not use the custom POST metadata route as an unreviewed fallback: metadata is a self-asserted claim. The v8 broad hosted baseline still HOLDs and should not be made to pass by retrofitting a consumed diagnostic.
- The pure comparator cannot establish observation timing, protected-alias settings or absence of intermediate alias movement. The future orchestrator must supply ordered, bounded, authoritative receipts and bind `selectedSource` to a one-use publication journal.
- Final focused checks passed 12/12 after the accessor assertion and manifest refresh. The full suite passed 2,382, skipped 2, failed 0; typecheck, both manifest checks, live-boundary and diff checks passed. Lint had zero errors and the same 20 warnings. A generated `.next/types` duplicate caused an initial typecheck failure; two byte-identical ignored duplicate files were removed, then typecheck passed. This was not a source defect. Local build passed at `aba39d7` before this pure unit; no app route changed.
- After Git read-runner recovery, its focused checks passed 7/7 with an offline HTTPS helper dispatch. The full suite passed 2,384, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. No app route changed. The prior full-suite/lint result above is historical context; this is the latest test result.
- After the coordinator unit, focused coordinator checks passed 8/8. The full suite passed 2,398, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. No app route changed. This is the latest test result.
- After the pure Git contract correction, focused contract checks passed 5/5. The full suite passed 2,403, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. No app route changed. This is the latest test result.
- After native read-port correction, focused contract/read checks passed 10/10 with zero skips. The full suite passed 2,408, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. No app route changed. This is the latest test result.
- After the fresh-recheck coordinator correction, focused checks passed 10/10. The full suite passed 2,410, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result.
- After the disabled binding correction, focused checks passed 6/6. The full suite passed 2,416, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result.
- After the injected-only push-port correction, focused Git/manifest checks passed 22/22. The full suite passed 2,420, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result.
- After the arming-preflight unit, focused checks passed 12/12. The full suite passed 2,424, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result.
- After the native arming reader correction, focused arming/source checks passed 22/22. The full suite passed 2,429, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result.
- After the disabled launcher correction, focused launcher/boundary/manifest checks passed 26/26; the full suite passed 2,432, skipped 2, failed 0; typecheck, both manifests, live-boundary and diff checks passed. This is the latest test result. The independent reviewer returned GO for this disabled unit, HOLD for actual arming/publication.
- `/usr/bin/git` is blocked by the unaccepted Xcode license. Local inspection uses `/Users/tobiastipper/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/git`. Do not accept the owner's license implicitly. The push policy is injected-only; the real process binding exists only behind the disabled launcher gate. A changed Codex runtime Git/GitHub CLI binary or helper will cause HOLD until separately reviewed and repinned.

## External state and remaining work

The live Shopify pre-launch email capture, double opt-in and authorized sender DNS were previously changed and tested using the owner's address; do not repeat the customer send without a new test purpose. Current hosted state may have drifted. No credential window is open. No new provider change, Preview publish, production deployment or purchase occurred in this checkpoint.

After Stage 3, the broader release plan still needs verified unified account/cart/orders/logout, inventory and order workers, supplier commercial/VAT basis, retail pricing and flash sales, affiliate attribution/discount/refund ledger, backup/restore, consent/email operations, research-backed score evidence review, consistent theme/customer journey, and production release checks. The offline Stage 6 pricing/affiliate modules are not connected to Shopify checkout or payouts. Obtain wholesale VAT basis, payment tariff, Shopify discount allocation and affiliate terms before live pricing. Product-level scoring remains on hold pending source labels/Path A evidence.

## Next action

1. Verify branch/HEAD/status and this disabled launcher commit. Before any connected-branch push, inspect the entire remote-to-selected-source diff, fresh Vercel project/build/root/production branch/Preview variables/protection/alias settings, and fresh Shopify/Supabase frozen state. Keep all activation controls disabled.
2. Prepare the separate local-only A worktree, review its **exact** two-file arming diff and fresh S/predecessor/manifest tuple, then obtain separate action-time approval. One push maximum; observe current Ready immutable Preview, exact Git proof and deployment-bound disabled runtime. Stop on uncertainty and leave activation held. The disabled-unit GO does not approve this action.
