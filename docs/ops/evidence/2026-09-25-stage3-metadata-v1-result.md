# Stage 3 preserved-fixture metadata V1 — one-run result

Status: **TERMINAL OBSERVATION / STAGE 3 HOLD**. Do not replay the V1 journal, rerun its launcher or apply the consumed arming preview. No deletion, provider change, account activation, purchase, customer message or production change occurred.

The owner approved one run after independent review and a read-only baseline. Immediately before arming, branch `codex/tll-integration` was at `06f5fc0d6541fba928169cde4834facbee3d26c7` with no tracked modifications and the six previously documented unrelated untracked paths. The reviewed patch SHA-256 was `39aa4389087247e627ca40435817c11d9e96278bd08e81a5024be8165fa50d38`; `git apply --check`, the disabled manifest and live-boundary checks passed. A descriptor-bound metadata baseline found the fixture directory, main file, sidecar and exact entries at their pinned identities. The user-domain default and list commands reported only the expected login Keychain. The new journal/binary/receipt were absent.

The exact four-change patch was applied locally. Armed Swift source hash `c496af448c15b2165716b061c528ad0e713efd26f2f84c9e5f781d33ee7046e7` and launcher hash `d62df689ad206491538c902f71e1688b6a702f18411af951bdf9404ce325634c` matched the review record; the activation manifest checked. The builder compiled once to a separate private arm64 ad-hoc binary, verified its strict code signature and recorded binary SHA-256 `3a854e15f89e5e7ee4738eef488183f6f4015209cec1b093de04e8b2fb22599d`. Build readback passed. A final read-only fixture and Keychain baseline passed. The fixed launcher was then invoked **exactly once**.

The launcher returned `{ "status": "OBSERVED", "category": "SEARCH_PATH_MISMATCH" }` in under one second. Its new mode-0600 journal `../implementation-state/staging/tll-provider-keychain-fixture-metadata-v1.json` is terminal with that category, bound to the expected source and binary hashes; its recorded duration is 619 ms. It is consumed even though the observation is useful.

The classifier checks default-path readability, search-list readability, default-path equality, then search-list equality in that order. Thus this category means the native default Keychain path matched the exact expected login path and the native search-list paths were readable, but the resulting ordered list was **not exactly** the one-element expected login-path list. It does **not** disclose whether the difference is count, order, another path or path representation. The `security list-keychains -d user` command still reported only the expected login Keychain afterward. [Apple's documentation](https://developer.apple.com/documentation/security/seckeychaincopydomainsearchlist%28_%3A_%3A%29) distinguishes a specified preference-domain search list from the current-domain list read by `SecKeychainCopySearchList`; a domain or representation difference is a hypothesis, not an established cause of the old cleanup failure.

The four-change patch was reversed immediately after the one run. Both source switches are false again; disabled source hashes, activation manifest and live-boundary check match the pre-run state. Read-only postflight found the terminal private journal, preserved private binary and receipt, no leftover run/build/check scratch directories, unchanged pinned fixture directory/main/sidecar/entries, and the same default/sole user Keychain output. The fixture files were not read for contents or removed. The old V1 cleanup journal remains untouched.

**Learning and next gate:** The old cleanup check assumed the native current-domain search list would equal the CLI's explicit user-domain list. This one-run observation shows that assumption is false in this process, but not why. Do not weaken the exact equality test by guesswork or retry cleanup. Design the smallest new read-only categorical observation that distinguishes list count, membership, path equivalence and preference domain without printing paths or reading items; consider an explicit user-domain read alongside the current-domain read. Independently review it, prove it offline, and seek a new action-time approval only if it materially changes the recovery decision. A later cleanup must compare an appropriately scoped, immutable baseline before and after, preserve any unrelated Keychains, and separately classify API and restoration outcomes. Stage 3 provider/account/cart remains HOLD.
