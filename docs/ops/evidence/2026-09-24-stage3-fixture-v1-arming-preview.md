# Stage 3 synthetic fixture V1 — isolated arming preview

The disabled integration source is committed at `36e174b` with the checkout-binding documentation at `c128565`. The exact private arming preview is `../implementation-state/staging/tll-fixture-recovery-arm-v1.patch`, SHA-256 `06f55f69798b6aa482d797928d1670bcb96bb751026b503234c62006a14c6da4`. It changes only two recovery gates from false to true and the two matching activation-manifest SHA-256 values. `git apply --check` passed in the original `implementation-integration` checkout.

The preview was applied **only** in isolated sibling `implementation-fixture-recovery-arm-v1` to build and verify the native artifact; neither the helper nor the live launcher was invoked. The source SHA-256 was `1c41c6fa064f2279c905b8fc4f40de935e6c1db66577deaf7aadb4b73898a255`, and the private armed binary SHA-256 was `3771b2278bf15a205c879e86c5e2dc4a973098e4344304787a151cf304b9715f`. Its owner-only binary and receipt are shared under `../implementation-state/staging/`. Independent Astra review verified exact source/receipt/binary hashes, modes, arm64 signing identity and a strict code-signature check; it found no actionable defect in the artifact or patch.

The reviewer caught a checkout guard: native execution requires working-directory basename `implementation-integration`. The sibling was therefore disarmed after artifact review and verified clean with the live-boundary and activation-manifest checks passing. The original integration checkout remained disabled throughout, and the new recovery journal remained absent. No fixture deletion, credential access, deployment or production change occurred.

**Next gate:** refresh the exact local fixture and Keychain baseline; confirm the original integration HEAD and patch still match; secure an exclusive one-run recovery window and owner action-time approval. Only then apply the reviewed patch in `implementation-integration`, verify the existing armed artifact using `--check-armed`, invoke the dedicated launcher once, disarm, reconcile and record evidence. A failed or uncertain attempt must not be replayed.
