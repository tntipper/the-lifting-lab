# Stage 3 protected Preview disabled-runtime result

Owner approval covered one read-only V2 use of the retained Preview bypass against the fixed staging Vercel alias and immutable deployment. The independently reviewed V2 plan and script are held in private `implementation-state/staging/`; the real V2 journal was absent immediately before execution. A fresh signed-in Vercel view showed Ready Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` at source `abd5a5258dd1072daf83a4447ce7110465f445e1`, with the specified alias and immutable URL. Read-only remote Git showed `codex/tll-integration` still at S.

The isolated command ran **once**, exited 0 in 8.6 seconds and returned `RUNTIME_DISABLED_VERIFIED`, deployment `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b`, immutable URL `https://the-lifting-7kom7bvbo-my-lifting-lab-s-projects.vercel.app`, checks `3`. The script made ordered GET reads of `/api/staging/readiness` at alias-before, immutable URL and alias-after. Each response met its strict JSON identity and security-header checks and reported all four private/public account/cart controls as the boolean `false`. No redirect, proxy, write, purchase or customer communication was included.

The private mode-0600 journal `implementation-state/staging/preview-runtime-proof-v2.json` is terminal `RUNTIME_DISABLED_VERIFIED`, run `e95b04f0-bdcc-434e-8b8f-3c429cf8ac54`, started `2026-09-24T16:15:12.742292+00:00`, updated `2026-09-24T16:15:21.357303+00:00`, deadline `2026-09-24T16:15:57.742615+00:00`. It contains no bypass. Preserve this consumed journal; do not replay the check. V1's separately consumed offline-test journal also remains preserved.

This establishes a source-bound, protected staging Preview running with account/cart controls off at those three observation points. It does not activate sign-in, prove a live customer journey, or establish that the alias never moved between observations. Stage 3 provider normalization, subsequent controlled activation and end-to-end account/cart acceptance remain held under their separate plans and approvals.
