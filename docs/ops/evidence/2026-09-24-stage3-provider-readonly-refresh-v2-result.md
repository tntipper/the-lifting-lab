# Stage 3 provider read-only refresh V2 — one-run result

Date: 24 September 2026, 16:40 UTC. Staging project: `qdmvngjwkcsilzmqksme`. Production project `wrhgscovsgsudtedbljr` was excluded. Plan: `docs/ops/stage-plans/2026-09-24-stage3-provider-readonly-refresh-v2.md`.

The owner approved one read-only credential window after the exact local arming diff and dependency arrangement had independent review. The reviewer initially held the run because the sibling worktree could not resolve `@supabase/supabase-js`; a local ignored `node_modules` link to the integration checkout was added, and resolution, ownership and lockfile identity were checked before approval and execution. The final review returned GO. This prevented a predictable post-journal failure without spending a credential attempt.

Immediately before execution, the local arming worktree was clean at `4e53ae43b593bd65d77f20c085c593d32c243aa8`. Its canonical V2 journal was absent; the same absolute journal path resolved from both checkouts under an owned, mode-0700, nonsymlink directory. The fixed Supabase CLI Keychain item was present (metadata only), the bundled Python SHA-256 was `ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`, and the SDK resolved from the sibling worktree. The signed-in staging dashboard still showed `custom:tll-staging-subject-broker-v1` ENABLED and only the three expected CA/Edge-flag custom secret names, without the broker secret. The protected Ready Preview remained at source `abd5a5258dd1072daf83a4447ce7110465f445e1`; its four account/cart controls had been proved false in the separate disabled-runtime receipt.

The exact launcher was invoked **once**. It exited 0 with `READONLY_PROVIDER_PRECONDITION_VERIFIED`, the fixed staging project and provider identifier, and strict database receipt SHA-256 `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`. It used one fixed SQL read-only POST, an Edge secret-name GET, a temporary staging service-role key GET and an official provider GET. The injected contract validated the complete provider schema, including enabled/JWKS-present preconditions, and the strict database aggregate. No provider update path was invoked, and no raw provider value or credential was retained here.

The private journal `../implementation-state/staging/tll-provider-readonly-probe-v2.json` is consumed. It is mode 0600, run `b6716140-e02b-4a85-bf9b-20d50d3bb6da`, terminal `STOPPED_BEFORE_UPDATE` at `PROVIDER_PREREAD`, file SHA-256 `74c3c610a7d398e36e7e1c02261036bbaba281add14068ad7be084c6af88982c`. Never delete or replay it.

Immediately after the run, both local gates were restored to false, the manifest regenerated and `STAGING_PROVIDER_READONLY_LIVE_DISABLED`, `npm run check:live-boundaries`, the activation-manifest check and `git diff --check` passed. Disarm commit `1b9e532f71098f1a50304a626bb75b6960dce8db` has no diff from disabled base `40b1654` in the three arming paths. A separate signed-in dashboard read still showed the provider ENABLED and the same three custom secret names. The result was definite and read-only, so the plan's conditional second database aggregate was not triggered. The 28 focused tests had passed while disabled before arming; ordinary tests were not run while armed.

**Decision:** the V2 read-only precondition is PASS and terminal. Stage 3 account/cart remains HOLD. A provider normalization update to `enabled:false` and `jwks_uri:''` needs fresh pre-mutation hosted evidence, an exact mutation plan and diff, independent review and a separate owner approval. This receipt does not authorize mutation, sign-in activation or public launch.
