# Stage 3 fixture armed-build path: disabled preparation

The armed fixture build/check path and the launcher's armed-identity selection were prepared while both fixture gates remained false. This is a local preparation checkpoint, not a Keychain execution. No armed binary was compiled; no fixture was created; no real credential, provider, hosted deployment, customer account or cart was touched.

The armed builder has a separate fixed filename and receipt. It requires the production reader's gate to remain false and the fixture-native gate to be true before either build or check can pass. It compiles immutable private snapshots with `TLL_KEYCHAIN_FIXTURE`, never runs the binary, refuses an existing target, and checks source/binary hashes, file ownership/mode/link count, arm64 architecture and ad-hoc signing identity. A shared pure validator is used by the real check and injected tests. The launcher's existing false gate returns before any file or process work; once separately armed, it selects only this builder/artifact and parses the exact armed status, schema and signer through an injected module.

Verification on disabled final code: `npm test` passed 2,468 tests, skipped two, failed none. `npm run typecheck`, targeted ESLint, activation-manifest `--check`, `npm run check:live-boundaries`, disabled fixture build `--check` and diff check passed. The armed builder's `--check` returned the expected HOLD while the gate remained false. Injected tests covered exact accepted build identity, source/binary drift, signer mismatch, unsafe permissions, hard links, symlink/extra fields, cross-mode launcher identity rejection, missing/consumed journal and bounded timeout behavior. No ordinary test imported the live launcher or executed native fixture work.

Independent Astra review returned GO for committing disabled preparation after the positive-path validator and launcher-identity tests. Its final review found the armed/disabled selection consistent and no new actionable defect. Live execution remains HOLD: prepare the exact two-gate plus manifest-hash arming patch, refresh the local baseline, review that exact patch independently, obtain owner action-time approval, then allow at most one execution under the phase journals. See `docs/ops/stage-plans/2026-09-24-stage3-disposable-keychain-arming-design.md`.
