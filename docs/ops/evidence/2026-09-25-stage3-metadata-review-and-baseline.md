# Stage 3 fixture metadata: independent rereview and local baseline

Status: **READY FOR ACTION-TIME OWNER DECISION; STILL DISABLED.** No armed binary was built, no live diagnostic ran, no fixture or Keychain item changed, and no customer/production system was touched.

The first independent review of disabled source `01448f7` returned HOLD for two access-boundary gaps: pathname-based fixture child inspection before the journal, and binary/receipt content reads before file-identity checks. The corrected disabled source is `ffd509afe646c6298625332ca33e1c15975ef508`. The launcher no longer reads fixture children; native Swift performs descriptor-bound observation after durable dispatch intent. Build verification opens artifacts without following symlinks, validates opened files before bounded reads, checks a private copy with strict code-signature verification, and the launcher executes a private copy matching the verified digest. Two focused tests were added. The independent rereview returned **GO to baseline and approval**, explicitly not permission to arm or run. Its limits: it reused our passing test evidence and did not build or run armed code or access the real fixture.

The corrected focused suite passed 14/14; the full suite passed 2,508 with two skipped and no failures. TypeScript, targeted lint, activation-manifest and live-boundary checks passed. The [revised four-change proposal](../arming-previews/2026-09-25-stage3-metadata-ffd509a.md) is based on `ffd509a`; its temporary patch SHA-256 is `39aa4389087247e627ca40435817c11d9e96278bd08e81a5024be8165fa50d38`. Independent rereview recomputed its hashes and `git apply --check` passed. The original `aa1834f` proposal is superseded.

Read-only baseline after the rereview: branch `codex/tll-integration`, HEAD `e29fa7560a5626ff8482be372d107eab5d74d669` at observation time, no tracked modifications, and exactly the six previously documented unrelated untracked paths. Both diagnostic switches were false. The fixed new journal, armed binary and build receipt were absent. The parent and staging state directories were owner-only. `security default-keychain -d user` reported the expected login Keychain, and `security list-keychains -d user` contained only that Keychain. A descriptor-bound metadata check found the pinned fixture directory, main file, sidecar and exact two entries unchanged; it read no file contents. The manifest and disabled-boundary checks passed. This is a point-in-time baseline and must be refreshed immediately before any arming action.

Next, obtain **fresh action-time owner approval** for exactly one local read-only metadata diagnostic. If approved, refresh the local baseline and patch hash/application check, apply only the reviewed four-line proposal, compile the source-bound arm64 private binary once, verify the build receipt, invoke the fixed launcher once, classify the one-use journal and fixed native result, disarm immediately, and verify. Any mismatch, prompt, timeout, unclear journal or result is HOLD/UNCERTAIN; preserve the fixture and journal, do not retry. A `METADATA_MATCHED` result rules out only path and fixture-identity hypotheses and does not lift Stage 3 or authorize cleanup/provider activation.
