# Stage 3 synthetic fixture V1 recovery — disabled preparation

Status: **disabled implementation verified; live cleanup HOLD**. This work did not open or delete the preserved synthetic Keychain, change the user's Keychain search list, read a credential, deploy, or purchase anything.

The V1 fixture creation ended at `CREATE/COMPLETE/HOLD` because macOS created its main file as mode `0644`. The exact synthetic file and known empty sidecar remain under the owner-only cache directory; both original one-use journals remain terminal. The identity-bound cleanup plan is `docs/ops/stage-plans/2026-09-24-stage3-fixture-v1-identity-bound-cleanup.md`.

The new recovery has separate disabled JavaScript and Swift gates, a new one-use owner-only journal, fixed synthetic paths and identities, and three ordered native phases. It uses the Keychain API for the main file, never a filesystem unlink fallback. It separately reconciles the known sidecar and then removes only the same empty directory. It checks the default/search list against the single login Keychain at every mutation boundary. Slow preflights, binary checks and reconciliations cannot extend a phase deadline. Ordinary tests statically inspect the live launcher and exercise injected orchestration; the native offline test excludes Security calls.

Independent Astra review initially returned HOLD and identified test-launcher invocation, pathname hashing after descriptor verification, deadline renewal, weak native receipt checks, insufficient sidecar entry checks, and insufficient destructive-path tests. Those controls were repaired. A second review identified unreserved time for per-child binary verification and an interaction-restoration false PASS; both were repaired. Final Astra rereview returned **GO for disabled preparation only**. The remaining runtime uncertainty is how `SecKeychainDelete` handles this preserved file and sidecar. POSIX pathname deletion also cannot atomically compare inode identity, so the later window must be exclusive.

Verification after the final corrections: `npm test` **2492 passed, 2 skipped, 0 failed**; `npm run typecheck`, targeted ESLint, activation-manifest check, live-boundary check, disabled Swift compile and source-bound disabled binary check passed. Final disabled Swift source SHA-256: `243e3523b72775406eeb367e27101389c3f457b1df5ba34523981d5a868d668a`; private disabled binary SHA-256: `0ed06c5dc5edcf352a559229efdaf2bd74939b14cd74184c56c5070eb67be4a7`. The earlier disabled binary and receipt were retained under `.retired-54df4e0c` names. No armed binary exists at this checkpoint.

Fresh read-only inventory immediately before recording this result matched the pinned V1 directory, main file, empty sidecar and terminal journal labels; the new recovery journal was absent. Before any live cleanup, refresh that inventory again, review the exact arming diff and armed artifact independently, obtain owner action-time approval, run once only, then disarm and reconcile. A successful cleanup would permit *planning* a new synthetic qualification; it would not activate provider mutation or account/cart features.
