# Stage 3 staging broker rotation — 25 September 2026

The owner separately approved one staging rotation after the 17:31 UTC read-only baseline passed. An independent reviewer returned GO for the exact three-file arming diff: two literal switches and their two activation-manifest hashes. It was committed **locally only** as `77d23d3402523329ef12bc57b410187b0a3062fe` in `../implementation-broker-rest-arm-v1`; nothing was pushed or deployed. The checkout's missing libraries were resolved before execution by an ignored link to the main checkout's matching-lockfile `node_modules`, and the required imports resolved. The new phase and rotation journals were absent immediately before the single direct launcher invocation.

The launcher returned `ROTATION_VERIFIED` in 17 seconds. The private mode-0600 phase journal `../implementation-state/staging/tll-provider-broker-phase-v1.json` has outcome `VERIFIED`, final phase `HOST_NAMES_READBACK` and sequence 12. The paired rotation journal `../implementation-state/staging/tll-provider-broker-rotation.json` has state `ROTATION_VERIFIED`. Both carry run ID `73105c27-fdf3-441b-bc57-50538b93d968`; they are consumed and must never be replayed.

A different, independently reviewed, read-only reconciliation launcher then ran **once** from local-only arming commit `ed5cd194eb9d773828eec551f2004425474ed4e9` in `../implementation-broker-recovery-read-arm-v1`. Its terminal result is `CONFIGURATION_CONSISTENT_SECRET_UNPROVEN`. The mode-0600 one-use journal `../implementation-state/staging/tll-provider-broker-recovery-read-v1.jsonl` has two records and binds to the same phase run ID. This confirms the expected disabled custom-provider fields, broker secret-name presence in both hosts, unchanged strict staging database controls and disabled protected Preview account/cart flags. It cannot read back or prove equality of the write-only secret values, and no customer sign-in journey has yet been run.

Both local arming checkouts were disarmed and committed: rotation `8629e335f31452d605d17738a9b42315f69f5723`, reconciliation `330721254f7269cb81e895faa78660377d6f2ce1`. Each arming path is byte-for-byte equal to disabled base `ecab3442167b3d382841e0780430836ea8354f21`; both checkouts are tracked-clean. Their activation-manifest checks and live-boundary checks pass. The main integration checkout remains disabled and retains the six unrelated untracked paths.

No production setting, account/cart activation, purchase, customer message or supplier order occurred. The next separate gate is to prepare a bounded, protected staging activation and sign-in test. A real sign-in cannot run while the database, Edge, private server and public Preview controls remain off: `createStagingCustomerRuntime` requires both public and private customer flags, and the activation runbook orders database, Edge, server and public controls before the journey. The retained Preview bypass and other pre-existing access remain under their prior handling; the new one-hour Vercel token expires naturally. See `docs/ops/stage-plans/2026-09-25-stage3-activation-readiness-next-gate.md` for the prerequisite checks; this result does not authorise activation.
