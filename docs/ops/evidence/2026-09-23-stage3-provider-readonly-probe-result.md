# Stage 3 focused provider read — one-run result

Date: 23 September 2026, approximately 10:29–10:33 UTC. Plan: `docs/ops/stage-plans/2026-09-23-stage3-provider-readonly-arming-gate.md`. Staging target: `qdmvngjwkcsilzmqksme`; excluded production target: `wrhgscovsgsudtedbljr`.

The owner approved this specific one-run credential window. Before arming, the disabled boundary and activation manifest passed, the distinct probe journal was absent, and the existing Supabase CLI Keychain item was present without reading its value. A fresh staging dashboard view listed `custom:tll-staging-subject-broker-v1` ENABLED. The Edge custom secret table listed the three existing CA/edge-flag names and no broker client secret. A separate read-only CLI database aggregate showed exactly one staging environment row, zero excluded-production rows, 15 migrations and zero login-enabled staging roles. That CLI used `postgres`; it was **not** counted as the strict receipt.

The exact arming diff changed only the read-only launcher flag and the shared Keychain helper flag from false to true, plus their two generated activation-manifest hashes. Independent review returned GO on that exact diff. Armed commit: `391055b`. No ordinary tests ran while armed.

The launcher was invoked **once** and exited 0 with fixed output `READONLY_PROVIDER_PRECONDITION_VERIFIED`, exact staging target and provider identifier, and strict database receipt SHA-256 `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`. It made only the reviewed SQL read-only POST, Edge secret-name GET, API-key GET and provider GET; it contains no provider update path. No raw provider response, token, key or secret value was retained in this evidence.

The distinct journal `../implementation-state/staging/tll-provider-readonly-probe-v1.json` is consumed and must never be replayed or altered. It has mode 0600, terminal `STOPPED_BEFORE_UPDATE` at `PROVIDER_PREREAD`, and file SHA-256 `31aa37ec3a7a3b8cac8af8579e3e19c1815276eb97c636295275682ff7e4a7df`.

Immediately after the invocation, both source flags were restored to false and the activation manifest regenerated. The live-boundary check passed before reconciliation. A new dashboard page still listed the staging provider ENABLED and the same three custom secret names without the broker secret. A separate read-only CLI aggregate again returned one staging environment row, zero excluded-production rows, 15 migrations and zero login-enabled staging roles. The post-run focused tests passed 27/27; typecheck, both manifest checks, live-boundary and diff checks passed; lint had zero errors and 20 pre-existing warnings. The full disabled suite passed 2,362/2,362 before the arming commit; the disarmed source is byte-for-byte the previously tested source.

**Decision:** focused Supabase preconditions are verified. The provider remains enabled, so Stage 3 sign-in acceptance remains HOLD. The current Preview is still behind local reviewed source and the complete Vercel/surface frozen-state preflight has not been refreshed in this window. A provider-normalization mutation requires a separate fresh plan, exact diff review, approval and one-shot journaled execution. This read-only result does not authorize public launch.
