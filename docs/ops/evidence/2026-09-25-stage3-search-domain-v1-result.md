# Stage 3 search-domain V1: one-run result

Status: **TERMINAL OBSERVATION / DISARMED / STAGE 3 HOLD**. Do not rerun this launcher, reuse its journal or reapply its consumed arming patch. No Keychain item, synthetic fixture content, provider, customer system or production setting was changed.

The owner approved this separate read-only run after independent review of the disabled package and exact four-line arming patch. Source commit before arming was `87ee0563458c4eeab1da3cfba572159e95d78692`; the patch SHA-256 was `2ecb413361787b9357db36dad473bda42cc8de05c91a709e8c1783b26d4a1b53`. Fresh preflight verified those hashes, only six known unrelated untracked paths, the old terminal journal, owner-only private directories, absence of the new journal/binary/receipt, pinned synthetic fixture identities and entries, exact user-domain default/list login path, disabled live boundary and current manifest.

The four-line patch was applied once. The armed Swift and launcher source SHA-256 values were `374c9e79307616f75c09fca9c556418445d936a2b2a8b3e959fe07ca86ad0e53` and `25a17c9c3816ee97e6ec4e8c245a6c806a3cb2108c22cf7fef88ed5e3e457baa`; the generated manifest check passed. The builder compiled and verified one private arm64 ad-hoc executable with binary SHA-256 `598249064734bba3d9f4a74cda9455d8110f2bb5d619bfeca81250f7500f263e`. The fixed launcher ran **exactly once** and returned:

`DOMAIN_USER:CURRENT_2:USER_1:EXACT_1:SAMEFILE_1:USER_EXACT_1:LISTS_DIFFERENT:IDENTITY_COMPLETE`

This means the active native preference domain was user; the native **effective** search list had two readable file paths, one exactly the expected login path and one different file. The explicit user-domain list had one exact login path. The effective and explicit user lists therefore differed. The code reports no raw paths, file identities or Security errors. A separate read-only CLI comparison before the run showed one login entry in the user list, one different entry each in system and common, and no dynamic entry. The native diagnostic did not identify which domain contributed the second effective entry.

The existing recovery code in `scripts/staging-provider-keychain-fixture-recovery-native.swift` sets `tllExpectedBaseline.searchPaths` to **only** the login path and requires `tllBaseline() == tllExpectedBaseline` before `SecKeychainOpen` or `SecKeychainDelete`. Today's observed two-entry native effective list would fail that guard before deletion. This is a concrete present-day explanation for why that cleanup design cannot advance. It is an inference, not proof of the exact native state or failure point during the earlier consumed cleanup attempt.

The patch was reversed immediately after the single run. Both switches are false again; manifest, live-boundary and `git diff --exit-code` passed. The private new journal `../implementation-state/staging/tll-provider-keychain-search-domain-v1.json` is mode 0600, terminal and bound to the verified source/binary hashes. Its compiled binary and receipt are preserved; no build/check/run scratch directory remains. Descriptor-bound postflight confirmed the preserved synthetic fixture directory, main and sidecar identities/size/entries unchanged, the user-domain default/list still at the expected login path, and the old metadata V1 journal still present.

**Learning and next gate:** The old one-entry assertion conflated the effective search list with the explicit user-domain list. Do not merely change it to `count == 2` or skip the guard. First design and offline-test a recovery check that records and compares the complete native effective list and user-domain list before and after, preserving unrelated entries and settings. Independently review the small recovery change and its failure handling before proposing any new deletion attempt. The prior cleanup and diagnostic journals remain consumed; Stage 3 provider/account/cart remains HOLD.
