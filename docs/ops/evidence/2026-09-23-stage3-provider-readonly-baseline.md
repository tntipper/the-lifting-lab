# Stage 3 provider normalization — read-only baseline, 23 September 2026

Observation window: approximately 10:12–10:23 UTC. Sources: existing signed-in Chrome dashboards, local Git/manifest checks and the cached Supabase CLI using its existing sign-in. No credential was created or revealed, and no Keychain prompt appeared; no journal, hosted mutation, deployment, purchase or customer action occurred. Plan: `docs/ops/stage-plans/2026-09-23-stage3-provider-fresh-readonly-baseline.md`.

| Gate | Fresh evidence | Decision / limit |
| --- | --- | --- |
| Local source | `codex/tll-integration` at `a3f4c913ece24416cb655fb42bf0698f01e154a7`; provider and older observer launchers and both Keychain gates false. Activation and hosted-baseline manifests `--check` and `npm run check:live-boundaries` passed. | VERIFIED disabled. Unrelated untracked files and consumed journals untouched. |
| Supabase target/provider | Dashboard project `tll-staging`, ref `qdmvngjwkcsilzmqksme`, lists `custom:tll-staging-subject-broker-v1` as **ENABLED**. The read-only edit form showed JWKS URI populated, manual endpoints populated, blank scopes, identifier-as-display-name and “Allow users without email” on. Form closed without saving. | HOLD: enabled/JWKS are the normalization target. UI does not prove PKCE, hidden Admin response fields or full schema. The dashboard's `main PRODUCTION` branch label is within this separate staging project, not the excluded production ref. |
| Supabase Edge secrets | The staging project's Custom secrets table listed three names: `TLL_STAGING_POSTGRES_CA_PEM`, `TLL_STAGING_POSTGRES_CA_SHA256`, `TLL_STAGING_SUBJECT_BROKER_EDGE_ENABLED`. The broker client-secret name was not listed. The edge-enabled value was not revealed; its displayed SHA-256 digest matches the local digest of literal `false`. | VERIFIED for names visible in this UI and flag digest, subject to an authoritative API inventory at action time. No secret value was opened. |
| Supabase database | The staging SQL editor redirected to the organization projects page and showed “You do not have access to this project.” A cached Supabase CLI 2.117.0 using existing sign-in listed exact project `qdmvngjwkcsilzmqksme`. Its fixed repository baseline SQL reached Management API but failed its strict `supabase_read_only_user` binding check because this CLI path runs as `postgres`. A separate `BEGIN READ ONLY` identity query returned database `postgres`, execution/session role `postgres`, one exact staging environment row and zero excluded production rows. An aggregate-only read returned 15 migrations, five runtime roles, zero login/nonretired roles, zero runtime sessions, and zero enabled customer/cart/broker/provisional/bridge controls. | Fresh partial aggregate evidence. The strict read-only-user receipt, password/marker/edge checks and complete provider preflight remain **UNAVAILABLE**; do not call this a PASS receipt or substitute historical v5. |
| Vercel Preview | Signed-in `the-lifting-lab` deployment page showed latest `codex/tll-integration` Preview `4M6P7P6rbG9w2Q9kC6y65rRzWB3V` Ready, sourced from GitHub commit `536d36a515ad39449338748b5739b0dcdedf8256`. Branch alias was assigned to it. Local HEAD is `a3f4c91`, so Preview is behind the reviewed source. | HOLD for a current sign-in journey. The UI did not show exact deployment `meta.tllManifestSha256` or complete project/deployment API receipt; both remain UNAVAILABLE. |
| Vercel Preview variables | The Project variables UI showed branch-specific Preview entries for `codex/tll-integration`, including staging Shopify and Supabase configuration names. `TLL_STAGING_SUBJECT_BROKER_CLIENT_SECRET` was not visible in the Project list. No values were revealed. | Partial name evidence only; no API-complete inventory or value/flag proof. |

**Decision: HOLD on provider arming.** The enabled/JWKS provider is an expected repair target, but the mandatory full frozen-state receipt and official provider Admin read are missing. The current Preview also cannot support an end-to-end sign-in acceptance check. The next unit should prepare a bounded, secret-redacted official provider read and finish the strict database checks using a supported identity, without weakening their original binding. Any new credential/permission window requires its own action-time gate. Independently review a minimal arming diff only after those facts are complete. Do not replay the consumed v8 observer or use a dashboard edit as a substitute for the one-shot journaled change.
