# Stage 3 — Slice 1 evidence + Slice 2 staging composition

Date: 2026-09-21  
Branch tip at plan start: `78e4bce8415936e82eaf4a88d2c06d26abcdcc13` (`codex/tll-integration`, Gen 19 SUCCESS then disarm PR #46 — gates OFF)  
Protocol: `docs/ops/project-stage-execution-protocol.md`  
Programme: `TLL-Unified-Commerce-Implementation-Plan-2026-09-15` / takeover 2026-09-20 (Toby approved Stage 3)

## Intended outcome

Close **Slice 1** with an honest, secret-free re-verification of Gen 19 against the tip (no paper PASS), and land the smallest coherent **Slice 2** unit: staging-only server composition with separate custody, least-privilege purpose pools, fail-closed missing config, and production paths off. Stop before Slice 3 route-journey work.

## Scope of this stage

| In | Out |
|----|-----|
| Slice 1 evidence artefact (PASS/GAP with file/test pointers) | Slice 3 — mount/prove `/auth/customer/` journey (routes may already exist as thin wrappers; this PR does not expand or claim them) |
| Slice 2 staging customer composition + focused tests | Slice 4 — Shopify callback proof |
| `.agent/HANDOVER.md` Stage status | Slice 5 — cart bind |
| | Slice 6 — logout / orders / browser journey |
| | Purchases, customer email, Gen 20, live/run-live-once/keychain/Gen arming |
| | Production Supabase `wrhgscovsgsudtedbljr` |

## Authoritative starting state

- Staging project: `qdmvngjwkcsilzmqksme`
- Gen 19 window: `51809dd4-bd4b-44c7-8609-7dd8ca063679`
- Package: `tll-staging-generation-19-credentials/v1`
- Observed live outcome (then disarmed): `CREDENTIALS_VERIFIED_CONTROLS_DISABLED` → tip status `CREDENTIALS_VERIFIED_CONTROLS_DISABLED_NO_REPLAY`
- Evidence: `docs/ops/stage-plans/2026-09-21-generation-19-credentials-verified.md`
- Manifest: `config/staging-account-activation-manifest.json` → `generation19Successor.status`
- Native gates: `generation19Armed: false`, `generation19ReplayPermitted: false` in `config/project-stage-gate-policy.json`
- Five DB purposes (Gen 19 identities): `customer`, `cart`, `broker`, `provisional`, `bridge` — `scripts/staging-generation-19-credentials.mjs` `IDENTITIES`

## Slice 1 — five-purpose gateway acceptance (re-verify vs tip)

### What Gen 19 proved (PASS)

| Criterion | Evidence |
|-----------|----------|
| Controlled staging DB connection on `qdmvngjwkcsilzmqksme` | Live summary in credentials-verified doc; status `CREDENTIALS_VERIFIED_CONTROLS_DISABLED` |
| Five purpose LOGIN roles provisioned/verified | `IDENTITIES` in `scripts/staging-generation-19-credentials.mjs`; receipt `runtimeCount: 5` |
| Connection verification exercised purpose isolation checks (`identity`, `membership`, `matrix`, `own_probe`, `table_denial`, …) | `scripts/staging-generation-19-transport.mjs` / database transport connection-failure allow-list |
| Controls disabled during verify | Receipt `controlsEnabled: false`; status name `CREDENTIALS_VERIFIED_CONTROLS_DISABLED` |
| Window closed; no replay; gates OFF on tip | Manifest `…_NO_REPLAY`; policy `generation19Armed/ReplayPermitted: false`; PR #46 disarm; `npm run check:live-boundaries` |

### What remains GAP for “repeatable restricted runtime access + authenticated five-purpose gateway”

| Gap | Why not PASS | Pointers |
|-----|--------------|----------|
| Gen 19 is single-shot / RETIRED | Repeatable app runtime access is not the retired live window; replay forbidden | credentials-verified doc “Replay: Forbidden”; manifest `NO_REPLAY` |
| DB probe ≠ application gateway | Gen 19 proved credential/connection verification, not HTTP `/auth/customer/` or cart bind journeys | Stage 3 slices 3–6 |
| Tip customer composition uses four admission purposes | Cart purpose is a **separate** composition (`lib/commerce/staging-cart-server.ts`), not inside `createStagingCustomerRuntime` | `lib/server/staging-customer.ts` `PURPOSES`; cart server `purpose: 'cart'` |
| Controls were disabled for Gen 19 verify | Does not prove controls-enabled gated access | Receipt `controlsEnabled: false` |
| No live re-probe on this tip | Correct: gates stay false; no Gen 20 | Policy + boundary check |

**Slice 1 verdict:** connection provenance **PASS** (Gen 19 + disarm tip). Integrated five-purpose **application** gateway **GAP** (deferred to Slice 3+). Do not paper over GAPs.

## Slice 2 — staging-only server composition (acceptance)

Concrete acceptance for **this PR only**:

1. `createStagingCustomerRuntime` composes reviewed staging config from server env; **no secrets in source**.
2. Missing / production / malformed / overlapping config → **`null` (controlled unavailable)** before pools or vaults are created.
3. Custody surfaces are separate: **token**, **provisional**, **cookie**, **final** vaults (distinct key ids and key material).
4. Purpose pools least-privilege for admission: **customer**, **broker**, **provisional**, **bridge** only — **cart excluded** (cart composition remains `lib/commerce/staging-cart-server.ts`).
5. Fixed staging origin / issuer / client / project ref checks; production project `wrhgscovsgsudtedbljr` rejected.
6. Synthetic execution on; live provider paths off (`liveEnabled: false` / equivalent).
7. Focused tests green; `npm run check:live-boundaries` PASS.
8. **Stop before Slice 3:** no new `/auth/customer/` route mounts or journey claims in this PR (existing thin wrappers, if any, are out of Slice 2 acceptance).

## Assumptions requiring evidence before mutation

- Tip SHA matches Gen 19 disarmed integration tip (verified by `git rev-parse` at plan start).
- Ordinary tests run only with native gates disabled (`check:live-boundaries`).
- No live launcher, keychain, or Gen arming in this stage.

## Files / systems that may change

- `docs/ops/stage-plans/2026-09-21-stage-3-slice-1-2.md` (this plan)
- `docs/ops/evidence/2026-09-21-stage-3-slice-1-five-purpose-acceptance.md` (and/or JSON sibling)
- `.agent/HANDOVER.md`
- `lib/server/staging-customer.ts` (composition inventory / custody locking only if tests require)
- `tests/staging-customer-runtime.test.mjs` (focused Slice 2 locks)
- Optionally `docs/ops/evidence/2026-09-21-stage-3-slice-2-composition-acceptance.md`

External systems: **none**.

## Pre-mutation checks

| Check | Proof |
|-------|--------|
| On expected tip / branch | `git rev-parse HEAD`, `git status` |
| Gates off | `npm run check:live-boundaries` |
| Existing composition tests baseline | `node --test tests/staging-customer-runtime.test.mjs` (and siblings if touched) |

## Failure / stop / recovery

Stop on unexpected hosted state, enabled gates during ordinary tests, desire to arm Gen 19/20, or scope creep into route journeys. Preserve evidence; do not retry live windows. Recovery: disarm if somehow armed (must not happen here); leave Gen 19 retired.

## Independent review point

PR diff: stage plan + Slice 1 evidence + Slice 2 composition/test changes only. Reviewer confirms no live arming, no production project, no purchase/email paths, gates remain false.

## Execution

1. Commit this plan.
2. Add Slice 1 evidence (secret-free).
3. TDD any Slice 2 composition lock (failing → passing).
4. Run boundary check + focused tests.
5. Update HANDOVER; open PR against `codex/tll-integration`.

Maximum attempts: one documentation/composition PR cycle (no live attempts). Deadline: this agent turn.

## Post-execution

- Secret-free evidence on disk
- HANDOVER Stage status: connection proven; integrated journey next; Gen 19 tip SHA
- PR/compare URL + tip SHA in report
