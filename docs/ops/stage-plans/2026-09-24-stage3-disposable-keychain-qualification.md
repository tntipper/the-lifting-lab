# Stage 3 disposable Keychain qualification — proposed next gate

Disabled preparation is complete and verified separately in `docs/ops/evidence/2026-09-24-stage3-disposable-keychain-disabled-preparation.md`. This document remains a proposal for a later one-run live fixture; it is **not** an execution authorization. The disabled-build checker intentionally refuses an armed source, so an independently reviewed armed-build design and exact execution plan are still required before any fixture keychain is created.

## Outcome and boundary

Qualify the reviewed native reader's real macOS Security.framework interaction behavior using a newly created, disposable file-based keychain containing only synthetic, non-secret text. Establish whether allowed, denied, missing and locked reads return fixed categories without presenting an authentication prompt or reaching any real credential. This does **not** qualify access to the existing login keychain or close the provider incident.

Current checkpoint: integration branch `codex/tll-integration` has disabled-reader commit `bd811df`; production guard remains false and the reader is not wired to a launcher. The one-use readiness receipt is consumed; provider-mutation receipts remain absent. Before implementation or execution, verify branch, HEAD, worktree, source/binary hashes, all disabled gates, private directory ownership/modes and exact receipt state again. Preserve unrelated untracked files and all historical receipts.

## Preparation before any Keychain mutation

1. Add a **fixture-only** compilation path that reuses the reader's classification, interaction-control, buffer and pipe logic but pins unique synthetic allowed, denied and absent selectors and an owner-only disposable keychain path. It must be impossible for this fixture build to select the three production services or `~/Library/Keychains/login.keychain-db`. Keep the production reader disabled. If source changes, rebuild and verify a new disabled binary; do not reuse the current one as matching evidence.
2. Add a bounded fixture driver with a private mode-0600 one-use parent intent journal and a fixture-only native phase journal. Its only live entry point must be a dedicated `*-live-launcher.mjs`. The native journal must bind to the parent's run ID, source/binary hashes and absolute deadline; durably record each effective operation before it starts, its completion category, and the cleanup phase. A timeout leaves the last durable phase as uncertainty. The driver must not use a shell command that puts a password in arguments, env, logs or Keychain Comments. Fixture values are non-secret but should still stay out of user-facing output. No native fixture process may run from ordinary tests. Add injected tests for path/selector pinning, both one-use receipts, complete/short/zero writes, deadlines, output allowlist, exact fixture-byte comparison before wiping, early stopping, ACL selection, baseline drift, and cleanup refusal on identity mismatch. The driver may emit only fixed categories and hashes of non-secret fixture identifiers, never raw fixture values.
3. Limit repository edits to this plan; `scripts/staging-provider-noninteractive-keychain.swift`; one fixture-only native setup/cleanup source and its native phase-journal source (these may be the same file); one fixed fixture build/check script; one dedicated fixture `*-live-launcher.mjs`, a separate injected-only session module and one-use parent phase-journal module; corresponding injected tests; `scripts/staging-account-activation-manifest.mjs`, its generated manifest and tests; `scripts/staging-live-boundary-check.mjs` and its tests; later evidence and `.agent/HANDOVER.md`. Run `npm test`, `npm run typecheck`, targeted ESLint, activation-manifest `--check`, `npm run check:live-boundaries`, fixture-build `--check` and `git diff --check` while all live gates are off. Ordinary tests may compile and run a separate `TLL_FIXTURE_NATIVE_TEST` main that uses only injected journal ports; they must never create a Keychain or execute the production or live fixture main.
4. Before execution, replace this proposal with an exact plan naming the final source hashes, compiled fixture identity, private directory and exact filename, creation/access/read/lock/delete calls, four-case read budget, one execution maximum, phase deadlines, expected return categories and failure recovery. Independently review the disabled diff and exact plan. Hosted baseline and provider reconciliation are not applicable to this wholly local fixture; local default-keychain/search-list metadata reconciliation is mandatory. Get separate owner action-time approval before creating the disposable keychain.

## Proposed one-run sequence after approval

Every native setup, read and cleanup process must save the prior legacy interaction setting, successfully set `SecKeychainSetUserInteractionAllowed(false)` before a potentially interactive operation, and restore the prior value on normal completion. Use Security.framework `SecKeychainCreate` with `promptUser=false` and a non-null in-memory synthetic password to create one file-based keychain **outside** the user's login-keychain path, under a new owner-only directory. The filename must not resemble the special login or System keychains. Capture default-keychain identity and search-list metadata before creation and immediately afterward; stop before reads if either changed. Never call a search-list setter.

Create two synthetic generic-password items with explicit access controls through an API accepting `SecAccessRef`, such as `SecKeychainItemCreateFromContent`. Trust the exact compiled fixture-reader identity for the allowed item; trust no applications for the denied item. Verify the decrypt-permission ACL metadata before either read, rejecting an unintended decrypt grant. Use a third selector that is absent by design. With the fixture reader constrained to this exact temporary keychain, allow at most four reads: unlocked allowed, unlocked missing, unlocked denied/interaction-required, and locked allowed. Stop qualification reads on the first unexpected category. Check successful bytes against the expected synthetic value inside the native process before wiping them; exit status alone is insufficient. This qualifies in-process read/classification behavior, **not** the production private-pipe transfer, which remains a separate integration gate. Bound the native child by the parent deadline and discard/wipe captured output on failure. `SecKeychainLock`/explicit-password `SecKeychainUnlock` apply only to this disposable keychain. `promptUser=false` suppresses only the creation dialog; the interaction switch and deadline cover the remaining native operations. An unexpected macOS dialog is a stop condition, never something to answer or accept.

After the one run, stop regardless of outcome. Reserve a bounded, journaled cleanup phase for known failures after the child has exited. Recheck the particular default-keychain identity, search-list metadata and fixture path/reference; do not claim all login-keychain state was observed. Delete only a verified fixture reference/path identity with `SecKeychainDelete`, then verify the returned path and any resulting fixture files are gone. Do not restore an old search-list snapshot over concurrent changes. If a child times out or its effects are uncertain, stop and preserve the private fixture for separate read-only reconciliation before any further mutation. If cleanup fails, preserve the private file and record HOLD; never delete by an unverified path or run a second test to diagnose. Disarm any local fixture gate and record a secret-free receipt, review outcome, and update incident evidence and `.agent/HANDOVER.md`.

## Stop conditions and remaining gates

Stop before reading on any path/selector mismatch, missing private-directory ownership or mode, source/binary hash mismatch, unexpected UI, blocked child beyond deadline, unexpected return value, changed default keychain search list, cleanup failure or receipt replay. No production provider, Vercel, Supabase, Shopify, real Keychain item, production deployment, purchase or customer message is in scope. Even a full fixture PASS only qualifies the disposable setup; exact real-item ACL access, launcher integration and parent failure behavior need separate reviewed gates and explicit approval before provider mutation.
