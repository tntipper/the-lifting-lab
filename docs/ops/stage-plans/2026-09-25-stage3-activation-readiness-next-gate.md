# Stage 3 protected activation: next readiness gate

**Status: preparation only.** The 25 September broker rotation and its separate read-only reconciliation passed their bounded checks and are consumed. This document does not approve a provider change, database credential window, deployment, customer sign-in, email, purchase or production change. Preserve the six unrelated untracked paths named in `.agent/HANDOVER.md`.

## Why the sequence changed

The earlier rotation handover incorrectly put a real sign-in test before account/cart activation. Source inspection shows `lib/server/staging-customer.ts` returns no staging runtime unless the private and public customer flags are enabled, and `lib/identity/staging-customer-ui.ts` hides the entry until the public flag is compiled into a Preview. The disabled Supabase provider also cannot exchange a customer session. The source-pinned activation manifest orders database controls, Edge, private server, public build-time flags and only then a no-purchase journey. A browser attempt against today's held Preview would be a meaningless failure, not a broker-secret test.

## Facts to preserve

- The provider remains disabled; its expected nonsecret settings and both broker secret names were reconciled, but the two write-only secret values cannot be compared by readback.
- All five staging database controls and the protected Preview account/cart flags were off at the completed read-only check. No customer sign-in has been demonstrated.
- The existing protected Preview contains the customer routes at source `abd5a5258dd1072daf83a4447ce7110465f445e1`, but its flags are off. Local `codex/tll-integration` has advanced since that source; any new build must pin the exact intended source, rather than assume it is deployed.
- The current surface binding deliberately refuses `createDeployment()` because the connected Vercel repository identity is not pinned in the activation contract. The generated manifest records `deploymentCreationBindingImplemented: false`. An enabled Preview cannot be claimed or safely automated through that binding yet.
- **Partial read-only repository identity, 25 September:** the signed-in Vercel project's Git settings visibly show `tntipper/the-lifting-lab` connected. The local `origin` points to the same GitHub path, and `gh api repos/tntipper/the-lifting-lab` reports GitHub repository ID `1264363509`. This is enough to identify the candidate repository, but does not yet prove the Vercel project API's connected-repository ID/type and deployment-create contract. Do not write this number into the live binding until that independent Vercel read and exact API-shape review pass.
- The older generation-6 runbook describes the intended dependency order, but Generation 21 is already retired. Its fixed generation/window and earlier migration baseline must not be reused. A fresh successor package is required.

## Ordered, cheapest proofs before building the activation window

1. **Read-only project inventory:** confirm the candidate GitHub ID above against the official Vercel project API's connected-repository ID/type, then capture project/branch/alias, current Preview source and deployment, Supabase provider state, five database controls and zero sessions. Inventory required Shopify, Vercel and Supabase secret **names and scope only**; do not expose values. Check whether the confidential Customer Account client, Storefront token, database passwords, vault keys, CA and proof markers required by `createStagingCustomerRuntime` are actually present and current. This is a fresh observation, not a replay of either consumed broker journal.
2. **Resolve build transport while disabled:** pin the verified connected repository identity and implement/test the smallest fixed-target `createDeployment()` binding or a separately reviewed equivalent. Prove source commit, manifest hash, protected branch alias, immutable deployment ID, TLS and runtime readiness for both held and enabled builds. Stop if the host cannot provide that proof. Keep every live switch off.
3. **Prepare a fresh generation and recovery:** use the current hosted migration/role baseline rather than generation 6 assumptions. Qualify a successor credential package and recovery path with offline tests, strict one-use journals, bounded deadlines, independent review and no reusable approval. Include the provider's disabled-to-enabled transition and its full-field readback; the completed rotation did not enable it.
4. **Plan the hosted window:** require a fresh read-only baseline and review of an exact minimal arming diff before requesting action-time approval. In the approved window, establish disabled Edge and Preview, provision/verify restricted runtime credentials, enable the five database controls atomically, then enable Edge, private server flags and public Preview flags in that order. The Preview must be a **new** immutable deployment after final flag configuration, with alias and runtime proofs. Stop and use the reviewed recovery path on mismatch or uncertain acknowledgement; do not retry a consumed step.
5. **No-purchase acceptance:** only after all gates pass, use the owner's previously authorised `toby@theliftinglab.co.uk` test identity to check Shopify sign-in, unified session, saved research, guest/account cart choice, checkout-page handoff without submission, orders isolation and logout. A sign-in may send an authentication message to that owned address; no marketing or customer campaign is in scope. Preserve sanitized evidence, then apply the reviewed closeout decision for temporary controls and credentials. Do not claim Stage 3 complete from configuration names alone.

## Stop conditions

Hold activation if the repository identity cannot be pinned, required customer/runtime credentials are absent or stale, the provider remains disabled without a reviewed enablement, source/alias/deployment identity drifts, the database baseline differs, an old journal is present, a new deployment cannot prove its compiled public flags, or recovery cannot be executed safely. The broker rotation's `ROTATION_VERIFIED` result must never be replayed to repair one of these gaps.
