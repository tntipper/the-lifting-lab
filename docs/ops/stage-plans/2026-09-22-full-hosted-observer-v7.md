# Full hosted observer v7 — disabled preparation

## Outcome and scope

Obtain one secret-free, current staging PASS or HOLD receipt across the fixed
Supabase, Vercel and readiness-surface reads. A terminal FAILED receipt is
accepted as a truthful diagnostic; intent-only is an incident. No production
request, purchase, deployment, provider enablement, database write, Shopify
action, customer message or supplier order is authorized by this plan.

## Starting evidence

Repository `implementation-integration` on `codex/tll-integration`. The v6
mode-0600 journal is terminal FAILED with SHA-256
`faaf41b67a1d8f8ba1b9f42c744f3c77052c15f6819878b8c04e737af3706d54`;
it is never replayed or altered. Both native source gates and the manifest are
disabled. The v6 one-hour Vercel token's local Keychain item was removed and
the retained Preview bypass has no temporary `/usr/bin/security` allowance.
The v6 failure was a false pagination requirement; its disabled parser
correction, incident record and full 2,254/2,254 tests were independently
reviewed GO. See the v6 plan for the causal and recovery evidence.

## Preparation and pre-mutation checks

Change only the full observer's exclusive journal constant to v7, the
manifest journal path, the path assertion in its test, generated manifest,
this plan and handover. Keep targets, endpoint, body limits, timeout and
failure classification unchanged. Verify the v7 journal is absent and v6
remains mode 0600 and terminal. Check `pwd`, branch, HEAD, status; preserve
unrelated untracked iCloud copies. Run focused tests, the full disabled suite,
generated manifest check and `npm run check:live-boundaries`; independently
review the disabled package. Commit only these explicit files.

Before arming, obtain a fresh read-only staging identity check and the exact
Vercel Preview environment envelope (HTTP status, entry count, pagination
presence and structure only; never record names or values). Reconfirm the
broker secret names and activation controls remain absent/disabled while the
pre-existing provider remains in its documented held state. Prepare only the two native-gate booleans and generated
manifest pins, and obtain independent review of those exact bytes.

### Preflight finding before arming

The 2026-09-22 read-only project identity check returned Supabase HTTP 200 for
`qdmvngjwkcsilzmqksme`; Vercel's signed-in project view showed the expected
`the-lifting-lab` project and 16 branch Preview variable rows, with the broker
secret name absent. The exact Vercel API envelope still needs a fresh check in
the next approved token window. Supabase's provider GET returned HTTP 200 and
an additive `custom_claims_allowlist: []` field that the strict parser did not
recognize. The preflight provider read therefore failed locally. Its enabled
state and JWKS URI match the previously documented held provider state; this
was not evidence of a new activation. Amend the disabled package to accept
only an absent or empty `custom_claims_allowlist`; any nonempty or malformed
allowlist remains a hard failure. Add tests for the observed empty field and
counterexamples, regenerate pins, rerun disabled checks and independently
review the amended package before preparing an arming diff. The earlier v7
review covered journal rotation only and does not cover this new parser change.

The amended parser accepted the same read-only hosted provider response and
reported the known held state (`enabled:true`, JWKS configured), without
printing IDs or secret values. Focused 46/46 and full disabled 2,255/2,255
tests passed after regenerating both hosted-baseline and activation manifests.
The amended disabled package received independent review GO; this does not
approve arming or executing v7.

The separately approved one-hour v7 token window then passed its preflight:
the saved token exactly matched Vercel's one-time display; all three fixed
Keychain selectors were readable; Vercel project GET returned HTTP 200 with
the expected project and team IDs; the exact Preview environment GET returned
HTTP 200, 16 expected branch/Preview/type rows, no pagination property and
no broker secret name. Supabase secret-name GET also returned HTTP 200 with
the broker name absent. No observer journal was claimed in these reads.

## Execution and stop rules

At action time obtain fresh approval for a one-hour Vercel Access Token
scoped to `the-lifting-lab` and a temporary `/usr/bin/security` allowance on
the retained Preview bypass Keychain item. Store the token in the fixed local
selector, compare its saved value privately to the one-time browser display,
and preflight all three exact helper reads. If any check fails, disarm
without claiming the journal or making a hosted request. Do not retry blindly.

Commit the reviewed arming diff; invoke the dedicated launcher exactly once
with its 60-second deadline. Do not run ordinary tests while armed. Whether
it passes, holds, fails or becomes uncertain, stop after that attempt, disarm,
verify the disabled boundary and manifest, reconcile the mode-0600 journal
and hosted state separately, remove temporary local access and leave the
remote one-hour token to expire naturally. Preserve any intent-only journal
and diagnose it without replay. Record the result and update handover before
any downstream stage.

## Executed result and incident boundary

The independently reviewed v7 arming diff was committed as `898bf0d` and the
launcher invoked exactly once. It returned terminal `OBSERVATION_FAILED` with
`observation_validation_unavailable`. The mode-0600 journal has run ID
`c66a7537-a63b-4071-baab-cd3442516ad2`, SHA-256
`d53760204f68ca4949af4be340f2dadaf4e3442abe24b91a6eb66e5f6c4d6084`,
and no observation hash. It is consumed and must not be replayed. Source
gates were disarmed in `53ffb67`, boundary and manifest checks passed, the
temporary bypass reader allowance and local Vercel API Keychain item were
removed, and the remote one-hour token was left to expire naturally.

Separate read-only reconciliation confirmed the staging database receipt
still passes with 15 migrations, zero enabled controls, five runtime roles,
zero runtime sessions, zero execution edges and five operator edges. The
Supabase provider read, Supabase and Vercel broker-name reads, Vercel project
and Preview environment reads, and the alias/deployment/readiness/Edge
surface reads all succeeded when isolated. The surface returned HTTP
200/200/200/503 in its fixed order with disabled readiness flags. The
project and deployment repository IDs, Git provider, project and branch
matched. An independently assembled receipt from serial read-only values
produced a local HOLD with four reasons and passed the pure receipt
projection. A tracked-fetch serial read also succeeded.

The direct cause of v7's generic validation failure is **not established**.
The process weakness is that the composite concurrent path can return the
generic `observation_validation_unavailable` without preserving the failing
validation stage, despite its port-level fixed labels. The sequential
reconciliation narrows the issue to composition/concurrency or a transient
inconsistent observation, but does not prove either. Do not prepare or arm v8
until a disabled, synthetic reproduction identifies the cause and a
reviewed fixed-stage diagnostic or correction has a counterexample test.
Never rerun v7's launcher or use a new journal merely to discover the error.
