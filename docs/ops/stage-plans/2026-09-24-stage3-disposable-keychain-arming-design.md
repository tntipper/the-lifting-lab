# Stage 3 disposable Keychain fixture — armed-build design gate

## Intended outcome and boundary

Prepare a verifiable **one-run** executable for the local synthetic Keychain fixture, then qualify its four read categories without touching a real credential. Success requires the allowed read to return its exact synthetic bytes, the absent read to report missing, the denied and locked reads to report interaction required, cleanup to remove only the verified fixture, and both journals to finish PASS. This qualifies fixture behavior only. It does not close the legacy-login-Keychain incident or authorize provider normalization, staging account/cart activation, a purchase, production work, or customer communication.

This document is an arming **design**, not authorization to create a Keychain. Current disabled preparation is commit `61e7513fc64ded3d84da148c87a6c20064fcdfb4`; see `docs/ops/evidence/2026-09-24-stage3-disposable-keychain-disabled-preparation.md`. Recheck this identity and the worktree before making any arming edit. Preserve unrelated untracked files and all private historical receipts.

## Required implementation before an action-time request

1. Add a separate source-bound armed-build/check path. It must reject production-reader gate `true`, require fixture-native gate `true`, compile only with `-D TLL_KEYCHAIN_FIXTURE`, use a new fixed private filename/signing identifier (never replace a disabled or historical binary), verify arm64, ad-hoc signature, owner-only mode, single link, both source hashes and binary hash, and never execute the result. The existing disabled build and receipt remain immutable. The armed build's check result must be the exact identity accepted by the one-run launcher.
2. Prepare the smallest arming diff in this `implementation-integration` checkout because the native journal currently binds to that exact checkout directory name. The disabled launcher already selects the armed binary/checker and parses its exact identity when its gate is true. Change only the fixture-native and fixture-launcher gates to `true` and the activation-manifest hashes required by those two source changes. Keep the production reader, provider, account, cart, order and all other live gates false. Do not run ordinary tests while armed.
3. Before arming, add injected offline tests of the armed-build identity parser and the launcher's refusal on source/binary/signature drift, missing or consumed journal, pre-existing fixture directory, and timeout. Run the full tests, typecheck, targeted lint, activation manifest and live-boundary checks **while disabled**, then independently review the exact arming diff and build design. No ordinary test may import the live launcher or execute either live main.
4. Capture a fresh **local** baseline just before arming: branch, HEAD, status; exact disabled source/build hashes; both Swift and launcher gates; owner/modes of `../implementation-state` and its `staging` child; absence of the fixed parent/native fixture receipts and fixture directory; default-keychain identity and search-list metadata; no unexpected Keychain dialog. A hosted baseline is not relevant to this entirely local synthetic fixture. If any condition differs, stop and revise the plan.

## Single execution window after independent review and owner approval

Commit the exact reviewed arming diff locally. Compile the armed binary once to the new fixed private name, verify its build receipt and signature, and verify the launcher's expected identity against it. Invoke only `scripts/staging-provider-keychain-fixture-live-launcher.mjs` once, with no arguments or environment-supplied selectors. The parent journal is `../implementation-state/staging/tll-provider-keychain-fixture-v1.json`; native journal is `../implementation-state/staging/tll-provider-keychain-fixture-native-v1.json`. Neither may exist before the run. The parent has a 90-second absolute run deadline, including a 15-second preflight, up to 65-second native phase and 10-second local reconciliation. The native child limit is 60 seconds and at most four reads. A journal INTENT is durable before every effective fixture phase. No output may contain synthetic bytes, credentials or passwords.

Native order is: create the fixed fixture file; create allowed and denied synthetic items; verify ACL metadata; read allowed, absent and denied selectors; lock; read allowed while locked; unlock; delete only the identity-verified fixture. A durable journal intent precedes every effective fixture phase; metadata and interaction-control calls may also occur within or between those phases. Before each potentially interactive Security.framework operation, successfully disable user interaction and restore the prior setting afterward. Any unexpected dialog, category, identity change, deadline, journal error or cleanup uncertainty is a stop/HOLD, not a reason to retry. If the child may still be running or timed out, do not delete the fixture; perform a separate read-only reconciliation after the phase deadline. If known stopped with a cleanup failure, preserve the private fixture and document its identity for reviewed recovery. Never use a path-only deletion or restore an old search list over concurrent changes.

After the one attempt, independently read and classify both journals, fixture absence and the particular default-keychain/search-list metadata. Disarm the source and launcher, rebuild/check disabled identities if source changed, run disabled-boundary checks, commit the outcome and update `.agent/HANDOVER.md`. The one-use journal remains consumed even on failure. A PASS advances only to a separate real-item ACL/parent-pipe integration design; provider mutation needs its own fresh plan, baseline, review and authorization.

## Review/approval state

HOLD: the separate armed-build/check path and the launcher's gate-selected identity logic are prepared while disabled. The armed builder's `--check` and `--build` both return HOLD with the current false fixture gate. The exact two-gate arming diff is not yet applied or independently reviewed. No fixture Keychain execution is authorized by this document. Seek owner action-time approval only after final checks, hashes, baseline and independent review are available as a concrete package.
