# Stage 3 staging provider normalization — one-shot action gate (unarmed)

## Independent review HOLD and disabled repair

Independent review found that the current normalization preflight checks project/branch/repository but does not pin the exact Preview deployment ID, immutable URL or Git source, and the shared surface reader checks the alias only before reading the immutable URL. A moved alias could therefore pass the written source-drift stop condition. Arming is **HOLD** until disabled source is repaired and rereviewed. Allowed repair files are `scripts/staging-provider-normalization-preflight.mjs`, `scripts/staging-provider-normalization-live-launcher.mjs`, `scripts/staging-account-hosted-baseline-surface.mjs`, their focused tests and the generated activation manifest. Pin the reviewed deployment `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b`, source `abd5a5258dd1072daf83a4447ce7110465f445e1` and immutable URL `https://the-lifting-7kom7bvbo-my-lifting-lab-s-projects.vercel.app` before any bypass-bearing request. Add an alias-after check scoped to this normalization use, require the same identity at preflight and postflight, and test changed ID/source/URL plus alias movement. Keep all native gates false. If the Preview changes legitimately, revise this plan and review its replacement identity before arming.

The same review found the separate-process full-field comparison in the draft acceptance criterion unsupported: only an in-process complete provider baseline exists, and the durable intent stores a combined hash. The repaired acceptance wording below credits the in-session independent official GET for full-field comparison; a separate-process reconciliation checks categorical provider state and controls. If the in-session result is uncertain, do not claim complete field preservation until a separately reviewed baseline/digest mechanism is available.

## Result and scope

Disable only the existing `custom:tll-staging-subject-broker-v1` provider in Supabase staging `qdmvngjwkcsilzmqksme` and clear its JWKS URI, then prove every other official provider field was preserved. The exact SDK update payload must be `{ enabled: false, jwks_uri: '' }`; maximum dispatch attempts **one**. Acceptance is a terminal `NORMALIZED_VERIFIED` intent and phase journal, an independent official provider GET inside the bounded session showing disabled/JWKS-empty with all other fields unchanged, fresh frozen database/Edge/Vercel/Preview controls, and disarmed source. This does not activate sign-in or checkout.

Production `wrhgscovsgsudtedbljr`, Shopify, purchases, supplier orders, customer messages, broker key/secret rotation, feature activation, deployments and connected-branch pushes are excluded. Do not delete or replay any prior receipt. Do not log the JWKS URI, provider raw response, access tokens, service-role key or bypass value.

## Starting evidence and permitted changes

At local integration HEAD `50b536a` on branch `codex/tll-integration`, the V2 read-only probe passed once with strict database receipt hash `04ee2fe8a104a319c44ababed0faed0737b6d983fb724567da6b50c4f5ea0670`; its consumed journal is terminal `STOPPED_BEFORE_UPDATE`. The signed-in staging UI still showed the provider enabled and the three expected custom secret names. The protected Ready Preview was last proved at immutable source `abd5a5258dd1072daf83a4447ce7110465f445e1` with four account/cart flags false. These observations are a starting checkpoint, not a substitute for fresh mutation-time reads. Preserve the six documented unrelated untracked paths.

The disabled implementation is already committed: `scripts/staging-provider-normalization-{contract,native-port,journal,coordinator,preflight,phase-journal,session,live-launcher}.mjs` and `scripts/staging-provider-normalization-keychain.py`. Both mutation journal files `../implementation-state/staging/tll-provider-normalization-phase-v1.json` and `../implementation-state/staging/tll-provider-normalization-v1.json` were absent at plan preparation. All nine source gates were false; direct launcher returned `PROVIDER_NORMALIZATION_LIVE_DISABLED`; the activation manifest and live-boundary check passed. Focused normalization tests passed 35/35 while disabled. The only allowed local arming change is nine false→true constants plus corresponding generated manifest hashes in a local-only, fixed sibling worktree. No arming source may be pushed.

## Pre-mutation checks and review

1. Verify `pwd`, branch, HEAD/status and exact source/manifest diff. Resolve both mutation journals from main and the sibling checkout to the **same** canonical private parent; confirm ownership, mode 0700, no symlink substitution and both files absent. Use a fixed sibling worktree sharing the `TLL and Store` parent, not a managed worktree elsewhere. Ensure its own dependency imports resolve before any credential window; an ignored `node_modules` link to the integration checkout is acceptable only after ownership and lockfile identity checks. Verify bundled Python path and SHA-256 `ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`.
2. While every gate is false, rerun direct disabled launcher, focused normalization tests, activation-manifest check and `npm run check:live-boundaries`. Confirm all nine flags false and both journals absent. Do not run ordinary tests while armed.
3. Refresh read-only hosted identity and controls: Supabase staging project and official full provider GET; strict read-only database aggregate (15 migrations, five retired roles, zero enabled controls/sessions/execution edges, five operator edges); Edge and Vercel broker-secret-name absence; Vercel project/repository/branch/Ready Preview source; protected Preview alias and immutable disabled readiness; staging Edge/private/public flags false. Stop if any target, provider field, count, secret or source drifts. A prior one-hour Vercel token must not be assumed valid; obtain a fresh bounded token only within a separately approved credential window. Retained Preview bypass remains protected.
4. Prepare the exact nine-gate arming diff and generated manifest hashes, with no logic changes. Independently review the final diff, transport requests, two-field payload, one-update guarantee, provider full-field readback, both journal paths/deadlines, Keychain permissions and recovery. If review finds a defect, return to disabled source and repeat the relevant focused checks before arming. Commit the reviewed diff locally only. Obtain fresh owner approval for this **provider mutation** after the exact action is reviewable; the V2 read-only approval does not apply.

## One attempt and reconciliation

Invoke `node scripts/staging-provider-normalization-live-launcher.mjs` directly **once** from the reviewed local arming commit. The launcher starts a mode-0600 phase journal before Keychain access, records `PREFLIGHT` and `PROVIDER_PREREAD`, then durably records intent before `UPDATE_DISPATCH`. It checks freshness again immediately before the one SDK `updateProvider` call. Phase deadlines are 60/45/30/10/30/10/45 seconds in order; no retry or second dispatch on timeout, malformed response, lost acknowledgement or cleanup failure.

If the result is `NORMALIZED_VERIFIED`, disarm the nine flags immediately, regenerate and verify the activation manifest and live-boundary check, then reconcile from a **separate read-only process**: official provider categorical disabled/JWKS-empty state, strict database aggregate, broker-secret-name absence, disabled Preview and Edge controls. The bounded session's independent official GET is the complete field-preservation proof. Record only fixed categorical results, hashes and terminal journal metadata. If any result is uncertain, preserve the journals, disarm, classify `RECONCILIATION_REQUIRED`, and do not retry or automatically re-enable the provider. Record root cause and prevention before any successor stage. The provider may remain disabled as the safe held state.

Commit a secret-free outcome and update `.agent/HANDOVER.md`. Broker rotation, Stage 3 account/cart activation and customer journey acceptance require later separate gates.
