# Stage 3 local credential-readiness window (disabled design)

## Outcome and exclusion

Prove whether the exact provider-normalization Keychain helper can read **all three** required local credentials in launcher order, without revealing values or consuming either provider-mutation journal. This is a read-only local diagnostic. Success is a terminal private receipt at the final read phase with overall PASS, an absolute deadline and no value in output; the reviewed source and tests establish selector order and exact invocation parameters. A HOLD identifies the first failing read phase and fixed helper exit category; it does not trigger a retry or provider mutation.

No Supabase/Vercel network request, new credential, Keychain permission change, source push, deployment, provider change, account/cart activation, purchase or customer message is in scope. The readiness result alone does not authorize the provider change or close the incident's still-unproved root cause.

## Starting state and assumptions

The disabled readiness implementation is committed at `244f744138cd34ba4d0eb769b3e637d11e8a1937`; the local-only sibling `../implementation-provider-credential-readiness-arm-v1` starts clean at that commit. The separate provider-normalization sibling remains disarmed at `c08633e300ab22b8cc711c42d331bc0f7c1b1391`, and both mutation journals are absent. The incident is `docs/ops/evidence/2026-09-24-stage3-provider-credential-storage-incident.md`. Earlier direct reads passed under both normal and restricted environments, but the old exact helper had failed and could not report its cause. Do not infer cold-start reliability or token validity from those direct reads. An expired one-hour Vercel token remains suitable for testing local Keychain access to its retained item; this diagnostic cannot establish that its bytes still authenticate to Vercel. Do not create a replacement token for this access-only window.

## Allowed disabled implementation

Add a dedicated `scripts/staging-provider-credential-readiness-live-launcher.mjs` with a false native gate, a separate `scripts/staging-provider-credential-readiness-phase-journal.mjs`, an injected `scripts/staging-provider-credential-readiness-native.mjs` adapter and focused offline tests, activation-manifest source pins and this plan/handover. The launcher calls the same bundled Python executable, `-I -S` arguments, helper path, working directory, PATH/LANG-only environment, ignored stdin, 15-second parent process deadline and 4,097-byte output ceiling as the provider-normalization launcher. Helper's own `/usr/bin/security` timeout remains 10 seconds. It must create its own exclusive mode-0600 private phase record **before** any Keychain call, advance through three selectors in order, store a 50-second absolute deadline plus phase deadlines, and reserve the full 15-second child limit before each read. Mark any exceeded deadline HOLD; synchronous filesystem finalization may complete after the deadline, so monitor the receipt and never infer progress from silence. Keep both provider-mutation journals absent and untouched. Capture, validate and wipe every owned output buffer; report only fixed categories and rounded elapsed time, never credentials or raw stderr. Fail closed on a consumed/malformed/symlinked receipt, unexpected helper exit, timeout, invalid output, cleanup error or deadline breach. No automatic retries.

Offline tests must inject a fake child process and private temporary journal. They must verify ordering, timing, categorical failures, zero secret output, buffer wiping, mode, replay refusal, and that neither provider-mutation journal is touched. Do not import or invoke the live launcher from ordinary tests. Its orchestration should be injected from a separate pure module where needed. Run ordinary tests only while every native gate remains false. Regenerate the activation manifest and independently review the exact disabled diff, including its journal and all native boundaries.

## Later action gate

This diagnostic is an investigation inside the still-open credential incident, not the successor provider-mutation stage. Use only the fixed sibling `../implementation-provider-credential-readiness-arm-v1` on branch `codex/tll-provider-credential-readiness-arm-v1`, starting from disabled base `244f744138cd34ba4d0eb769b3e637d11e8a1937`. A fresh hosted baseline and hosted reconciliation are **not applicable** to this local-only, network-free diagnostic; they remain required for provider normalization. Before arming, verify the exact source commit and clean sibling, private directory ownership/mode, absence of all three relevant receipts, the bundled Python hash, Keychain item metadata and access-control settings. Record token expiry as known or unknown, without claiming API validity. If an item is missing, the existing Keychain ACL does not already allow the exact reader, or a prompt requires an unplanned permission change, stop and seek a separately reviewed plan; do not discover permission behavior by consuming the one-use receipt.

Prepare and independently review the exact arming diff **before** seeking fresh action-time owner approval for a single local credential-readiness window. It may change exactly three files: `scripts/staging-provider-credential-readiness-live-launcher.mjs` (`CREDENTIAL_READINESS_LIVE_ENABLED` false→true), `scripts/staging-provider-normalization-keychain.py` (`APPROVED_NATIVE_READ` False→True), and the two matching source hashes in `config/staging-account-activation-manifest.json`. The readiness phase-journal and session constants are not functional switches and remain false. Because the helper is also used by the provider-normalization and provider-readonly launchers, verify **both** of those launcher gates remain false; the other eight provider-normalization gates remain false while the shared helper is temporarily true. Prohibit direct helper invocation: its successful stdout contains credential bytes and must only enter the reviewed launcher, which wipes its buffers. Commit the reviewed arming diff locally only. Do not run ordinary tests while armed.

After approval and all preflight checks, invoke `node scripts/staging-provider-credential-readiness-live-launcher.mjs` **once** from that reviewed arming commit, with no new token, network request or provider action. Its only allowed receipt is canonical `../implementation-state/staging/tll-provider-credential-readiness-v1.json`; both protected mutation receipts `tll-provider-normalization-phase-v1.json` and `tll-provider-normalization-v1.json` must remain absent. Monitor only the secret-free receipt using `assessCredentialReadinessPhase` and its recorded deadline; silence or lack of a child process is not proof of a stall. Allow the 50-second absolute window and bounded finalization before reconciling. On PASS, HOLD, uncertainty or timeout, make no second attempt and preserve any consumed receipt. Disarm both switches immediately, regenerate the activation manifest, verify the disabled launcher and live-boundary check, commit the outcome locally, and reconcile the receipt categorically without exposing credential values. A PASS proves only that the three saved items were readable in that bounded window; it does not close the incident or authorize provider mutation. Provider normalization requires its own later hosted baseline, exact arming review and action-time approval.
