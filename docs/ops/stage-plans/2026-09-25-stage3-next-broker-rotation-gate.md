# Stage 3: next broker-rotation gate (preparation only)

**Status:** HOLD for execution. This plan does not authorize a credential window, provider update, account/cart activation, or Git publication.

**Completed offline slice (25 September):** The new injected-only phased session connects the existing rotation to the durable phase journal. It starts the phase record before acquiring any adapter, records each host/provider dispatch before invoking that adapter, validates the acknowledgement before recording it, binds both journals to one run ID, and closes adapters before a verified terminal record. Failed dispatch recording prevents the corresponding effect. Lost acknowledgements trigger the existing cleanup path; provider-update uncertainty preserves host values and requires reconciliation. Independent review found a timestamp mismatch between the two journals; the rotation intent now uses the durable phase intent timestamp, and advancing/backwards-clock tests check the actual records through the recovery assessor. Rereview returned GO for disabled preparation only. This has no Keychain reader, live launcher, process deadline, network transport or enabled switch. The next slice is a separate bounded launcher and exact disabled arming diff; only after its review, fresh hosted baseline and action-time approval may a credential window be considered.

**Launcher design constraint discovered before implementation:** The existing read-only recovery supervisor has a hard 70-second maximum, while the rotation phase journal permits up to 660 seconds for a Vercel staging or removal phase. The existing generic adapter executor caps an operation at 30 seconds and waits for it to settle after abort. Do not copy the recovery launcher's lifetime unchanged or let a timed-out host command race cleanup. The separate rotation launcher must define one finite whole-process budget and separate startup, host, cleanup and provider-update budgets, kill its complete process group when the parent deadline fires, and leave an interrupted phase for read-only reconciliation. Prove cancellation and cleanup ordering offline before any credential window.

## What is established

- The official one-use staging read on 25 September returned `SAFE_HELD_PROVIDER_OBSERVED`: the custom OAuth2 provider is disabled, the exact retained JWKS address is present, the broker secret name is absent, and database controls are off. Its journal is consumed. The earlier two-field update did not satisfy its acceptance condition and must not be replayed.
- The signed-in Supabase editor still showed the provider disabled with that address on 25 September. The Vercel dashboard showed protected Preview `dpl_9CFPQG6JChoGrkWidhh73BY1Qj1b` Ready from source `abd5a5258dd1072daf83a4447ce7110465f445e1`. These UI observations do not replace the official API or protected runtime proofs.
- The original disabled compatibility source is `adda1f54d060a4564325d5e5ae5109e812ec56de`. Its rotation code pins the retained address and omits `jwks_uri` from a future update. The subsequent disabled guard change requires all permitted starting settings before generating a credential; its focused/full tests, typecheck, targeted lint, both manifest checks and live-boundary check passed. Neither change has been deployed to Preview.
- Independent publication review found no actionable runtime defect in the 72-commit local/remote gap, but recommended **no automatic push**. The existing publisher has empty source-selection fields and its previous journal is consumed. A new publication package would be required if later proof actually needs the deployed source to equal local HEAD.

## Smallest next slice

1. Keep the current protected Preview. Its sign-in runtime and readiness contract are unchanged by the local retained-JWKS repair. Check the remote branch and deployment identity again immediately before any hosted read; stop if either moved.
2. **Completed in disabled source:** the rotation function now requires the complete permitted starting provider configuration **before** credential generation. It accepts the two scope states observed by the safe-held reader, while unexpected scopes, endpoints and other projected settings stop with zero generated credentials, journal or host writes. Focused and full tests passed; an independent review returned GO for disabled preparation. The eventual update still omits `jwks_uri` and the old failed normalization contract remains untouched.
3. Prepare one fresh, bounded execution package only after the review: exact source commit, isolated checkout, all required gates initially off, pinned targets, direct launcher, finite parent and child deadlines, no inherited credentials, and an exact minimal arming/disarming diff. In addition to the existing rotation journal, require a private durable phase receipt recorded before work and before each host write/provider update, with the deadline and categorical outcome. A separate read-only process must reconcile each phase after the run. Test startup and all dynamic imports offline before opening a credential window. A source-only state machine is not a live launcher.
4. Independently review the package and arming diff. Immediately before any run, obtain a new official read-only staging baseline and a protected Preview identity/readiness check. Require the exact disabled provider settings and retained JWKS address, absent broker secret names, database controls off, and four account/cart switches false. Bind the complete provider baseline to the package's pre-generation check; a mismatch stops without creating or staging credentials.
5. Seek separate action-time owner approval for the **specific** one-run credential and provider window. On an approved run, make at most one provider update. Verify every provider field and both host secret names through independent readback; record a terminal result; disarm. Never replay a consumed or uncertain journal. Account/cart activation remains a separate gate and requires an owner-email sign-in, logout and recovery journey without purchase.

## Publication decision

Do not publish `adda1f5` merely to repeat the current read-only provider check. If the next strict hosted baseline reports `CURRENT_SOURCE_NOT_DEPLOYED`, treat that as a source mismatch, not as a provider failure. Prepare a separate reviewed Preview-only publication selecting exactly `adda1f54d060a4564325d5e5ae5109e812ec56de` from remote predecessor `abd5a5258dd1072daf83a4447ce7110465f445e1`, with a fresh journal and post-deployment source/disabled-runtime proof. Never reuse the previous publication launcher state or journal.

## Stop conditions and evidence to keep

Stop on changed provider/Preview identity, source or manifest mismatch, any enabled account/cart control, missing dependency, stale token, Keychain denial, timeout, uncertain provider response, or incomplete readback. Preserve the private journal and report what was proved; do not retry under the same approval. This plan changes no hosted state and does not claim the real OAuth2 sign-in path has passed.

If a host write fails before the provider update, attempt removal within a separate finite cleanup budget. Only a confirmed absence of **both** secret entries permits `STOPPED_BEFORE_PROVIDER_UPDATE`. A failed removal, lost acknowledgement or exhausted cleanup deadline remains `RECONCILIATION_REQUIRED` and requires the separate read-only reconciliation process. Offline tests must cover all three cases; the existing focused tests do not.

**Next code slice:** implement the separate read-only hosted reconciliation and connect the phase receipt to a bounded disabled launcher. Do not arm the source-only rotation function or infer that the passing guard/journal tests qualify a credential window.

### Read-only collection core (disabled preparation)

The injected-only collection core now checks the phase and rotation receipts before any hosted read, rejects active/stale/invalid runs, and requests a pinned Preview read before provider, host-name and database observations. It supplies the reviewed deployment ID, immutable URL and Git source and checks the returned identity. The future adapter must bind these values at construction so the existing surface binding checks them **before** using the Preview bypass and checks the alias again afterwards; the collector cannot prove the ordering inside an arbitrary injected port. A 45-second maximum deadline aborts the collection, and its in-memory instance refuses a second call. It passes the observations to the recovery assessor without adding a write path. Focused tests cover held/active/malformed receipts, source drift, timeout, a non-returning Preview port and replay. Full suite 2,590 passed/two skipped; typecheck, targeted lint, both manifest checks and live-boundary passed. This is **not** a live one-use process: it has no durable read journal, credential owner or executable launcher. Next, connect only reviewed read-only bindings under a separate private one-use journal and parent deadline, then independently review the exact disabled package before any arming proposal.

### Next read-only journal slice

Add a new secret-free, one-use file for this recovery read rather than reuse any consumed provider or hosted-baseline journal. Claim it exclusively after a valid terminal phase/rotation preflight and **before** credentials or hosted reads. Keep the opened file descriptor for the whole attempt; append a categorical result and sync the file and containing directory. A missing terminal line, interrupted write, changed file identity, insecure file/parent permissions or malformed record must remain an uncertain consumed attempt, never an invitation to replay. The journal stores the phase run ID, fixed target, pinned Preview identity/source and timestamps, but no credential, provider payload or customer data. Prove claim/replay, interrupted finish, replacement and timeout cases offline. Then wire the journal to a disabled coordinator. Do not change the older journals or live launchers.

**Completed as disabled preparation:** The new journal, coordinator and read-only connector composition are implemented. The journal is claimed before connector acquisition and checks its named file both after disk sync and immediately before acquisition. The coordinator has a 55-second bound and disposes late or failed connector acquisitions. The collector's own 45-second bound now starts before awaited receipt reads, so cancellation cannot slip past it. The connector composition constructs the existing protected Preview reader with the exact deployment/source pin before any bypass-bearing request and exposes only provider, broker-name and database reads. It filters unrelated Supabase Edge secret names, which may legitimately exist. Independent review reproduced a file-swap race and a cancellation-order race; both were corrected and rereview returned **GO for disabled preparation only**. Focused tests passed 18/18 and full suite passed 2,605/two skipped. No credential or hosted access occurred.

**Next:** Build a separate disabled `*-live-launcher.mjs` with a strict local credential owner and process deadline. It must load and validate its imports offline before any credential window; keep its native switch off, never reuse older one-use journals, and undergo an independent review of the exact launcher and arming diff. A fresh official staging and protected Preview baseline plus separate action-time owner approval are still required before execution.

### Disabled launcher preparation

Create a new exact-selector Keychain helper and launcher, both with independent false gates, so changing them cannot reactivate an earlier consumed baseline helper. The launcher must check the safety manifest and fixed staging target, read the phase/rotation receipts through their existing local readers, claim the new recovery-read journal, and only then acquire the three credentials. Use bounded asynchronous child processes with a minimal environment, output limits and abort killing; pass credential buffers to the reviewed read-only bindings, which wipe transferred buffers. Import the connector modules in a fresh offline startup check before any proposed credential window. Add the helper and launcher to generated source pins and to the live-boundary rule that requires all ordinary launchers disabled. This slice must stop at disabled preparation; the later exact arming diff and its approval are separate.

**Completed as disabled preparation:** A dedicated launcher and Keychain helper were added with both native switches false. A separate supervisor sets a 70-second whole-worker deadline and kills the worker's process group; the worker watches the supervisor pipe and stops itself and its descendants if that pipe disappears. The liveness watcher is released after the read session so normal completion can exit. An independent reviewer reproduced and drove corrections for orphaned credential descendants, supervisor interruption and a normal-exit deadlock, then returned **GO for disabled preparation only**. Offline tests prove stalled startup, group termination, `SIGINT`/`SIGTERM`/`SIGKILL` supervisor loss and successful handshake completion. The full suite passed 2,610/two skipped, with typecheck, lint, both manifest checks and live-boundary passing. The launcher prints `BROKER_RECOVERY_READ_LIVE_DISABLED`; the Python helper exits before any Keychain read. No hosted or credential access occurred.

**Decision for the next gate:** This recovery reader is for a future terminal broker-rotation phase record. There is no new broker rotation attempt to reconcile now, so arming it now would consume credentials without answering a useful question. Keep its one-use read journal absent and both switches false. Next prepare and independently review the bounded **rotation** launcher that writes the existing phase receipt before each host/provider effect; use this read-only recovery package only after a terminal or uncertain rotation state exists. Fresh staging/Preview evidence and separate owner approval are still required before any credential or provider window.

### Read-only recovery assessment slice

**Completed in disabled source:** the injected-only recovery assessor and offline tests accept the one-use phase and rotation receipts plus four **read-only** observations: the official staging provider, broker secret **names** in each host, the staging database control receipt, and the pinned protected Preview readiness response. It may report a safe holding configuration only when the provider was never dispatched, both broker names are absent, the provider matches the exact disabled safe-held settings, database controls are off, and the four Preview account/cart flags are false. The phase and rotation receipts must share one run identifier, and rotation intent creation must fall between the journal's intent and first host-dispatch timestamps. If provider dispatch occurred, the write-only client secret cannot be proven by GET; even matching visible settings produce only `CONFIGURATION_CONSISTENT_SECRET_UNPROVEN`. An active in-deadline run must not trigger reads; a stale but unconfirmed stopped process must first be stopped and separately reconciled. This core has no credential reader, transport, launcher, file writes or mutation ports. Its own bounded read-only process and one-use journal are a following slice.

Acceptance passed: valid safe-held evidence, provider-dispatch uncertainty, unexpected host names/provider/Preview/database drift, unrelated/old/future receipt rejection, missing or malformed receipts, and active/stale-window refusal. The final full suite passed 2,587/two skipped, with typecheck, targeted lint, both manifest checks and live-boundary check. Independent security review found and resolved the receipt-binding and post-rotation provider-name gaps, then returned GO for disabled preparation only. No hosted action or owner input was needed for this offline slice.

### Phase-journal slice boundary

**Completed in disabled source:** this slice adds only `scripts/staging-provider-broker-phase-journal.mjs`, its injected offline test, generated safety-manifest hashes and this plan/handover. It does not connect the journal to a live launcher or call a host. The journal records fixed phase names, a run identifier, ordered timestamps, a deadline for each phase and a terminal category; it records no token, secret, URL or raw provider data. The relevant phase names distinguish **dispatch before effect** from acknowledgement for Vercel secret staging, Supabase secret staging, provider update and cleanup. An interrupted dispatch is therefore uncertain. A success terminal requires provider and host readback; a safe pre-update stop requires confirmed cleanup readback for every host whose staging was dispatched. Any uncertain result remains reconciliation-required and cannot be replayed.

Proof passed: exclusive mode-0600 creation, durable replacement, invalid transition and replay refusal, exact deadline/stale classification, success and cleanup branches, interrupted dispatch classification, malformed/symlink/insecure-file rejection, injected path replacement and write/fsync failure tests. Full suite 2,582 passed/two skipped; typecheck, targeted lint, both manifest checks and live-boundary check passed. Independent security review found and resolved a file-replacement race and a potentially blocking FIFO open, then returned GO for disabled preparation. The following slice will connect this journal to a bounded launcher and implement the separate hosted read-only reconciler; that connection needs its own review and fresh action-time approval before any run.
