# Stage 3 staging provider read-only refresh V2

## Intended result and acceptance

Obtain one fresh, secret-free official Supabase staging preflight before considering provider normalization. Reuse the reviewed injected probe and fixed Supabase binding, but give the disabled launcher a **new** one-use journal path `../implementation-state/staging/tll-provider-readonly-probe-v2.json` and the working, fixed bundled Python executable. Acceptance is `READONLY_PROVIDER_PRECONDITION_VERIFIED`, a strict database receipt hash, and a terminal mode-0600 `STOPPED_BEFORE_UPDATE` journal. The probe's complete official provider-schema validator must accept the exact enabled/JWKS-present staging provider while its SQL read-only aggregate proves 15 migrations, five retired runtime roles, zero controls/sessions/execution edges and five operator edges; Edge secret names must exclude the broker secret. This is pre-mutation evidence only.

## Starting state, exclusions and allowed changes

At local HEAD `e709bc7379c0bdfe1110e010068c7548f6f345d1`, branch `codex/tll-integration`, only six documented unrelated untracked paths exist. Remote `codex/tll-integration` and protected Ready Preview were last proved at immutable source `abd5a5258dd1072daf83a4447ce7110465f445e1`, with account/cart controls off. Signed-in staging Supabase UI still listed the provider ENABLED and no broker custom secret, but that UI is not official full provider/schema/database evidence. The 23 September focused probe journal is consumed and must not be replayed.

This V2 unit permits **no provider update**, account/cart/Edge activation, production change, deployment, purchase, supplier order or customer communication. The only source changes before any external run are `scripts/staging-provider-readonly-live-launcher.mjs` (new fixed journal path and fixed Python path), its hash in `config/staging-account-activation-manifest.json`, this plan and eventual evidence/handover. Keep both launcher and Keychain helper flags false while preparing and testing. Preserve all historical journals and unrelated untracked files. The later provider mutation uses a separate plan and approval.

## Pre-run checks and independent review

1. Verify path, branch, HEAD/status and the entire intended diff. Confirm all native gates false, both activation manifests fresh, live-boundary check passing, old journal preserved and V2 journal absent. Check the bundled Python symlink target/executable SHA-256 `ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`; if absent/changed, HOLD. System `/usr/bin/python3` is blocked by an unaccepted Xcode license and must not be used as fallback.
2. Inspect fixed request boundary: one SQL `POST` with `read_only:true` to the staging Management API, one secret-name GET, one API-key GET for the staging legacy service-role key, one official provider GET. No production project, write endpoint or raw response may be added. The Management token and service key are never printed or committed.
3. Run ordinary focused probe/boundary/manifest tests and direct disabled-launcher probe only while every gate is false. Offline tests use injected credentials and transport and private temporary journals; no test imports or arms the live launcher. Independently review the exact disabled diff, the credential/transport boundary, deadlines, replay and failure behavior. A GO for disabled preparation does not authorize execution.
4. Immediately before an external window, re-observe staging project identity, dashboard provider category and custom secret names, protected Preview source/alias and disabled runtime evidence without replaying consumed readers. Confirm the fixed Supabase CLI Keychain item exists without revealing it, V2 journal remains absent, and no relevant hosted drift. Obtain fresh owner approval for this specific one-run credential window. Prepare the smallest two-gate arming diff (read-only launcher and its own Keychain helper, plus generated manifest hashes), independently review it, then commit it in a local-only arming worktree at the fixed sibling path `../implementation-provider-readonly-arm-v2`. The sibling must share the same `TLL and Store` parent as `implementation-integration`: the launcher's `root/../implementation-state/staging/tll-provider-readonly-probe-v2.json` must resolve to the **same absolute private state path** from both checkouts. Before arming and again before invocation, compare both resolved journal paths byte-for-byte, resolve and inspect the parent directory (owned by this user, mode 0700, no symlink substitution), and confirm the single canonical V2 receipt is absent. A managed worktree elsewhere is HOLD; never allow it to create a second receipt namespace. Do not run ordinary tests while armed.

## One attempt, deadlines, recovery

Invoke the exact reviewed launcher directly **once**. The phase journal starts before Keychain access, records `PREFLIGHT` before the database/secret reads and `PROVIDER_PREREAD` before the official provider read, and has 60/45/30-second phase bounds. Any timeout, unavailable credential, unexpected schema/count/secret, journal mismatch, loss of acknowledgement or uncertain result consumes the attempt and requires separate read-only reconciliation. Never retry V2 or delete a receipt to proceed. Do not infer success from a dashboard badge.

Immediately disarm both flags and verify native boundaries before any reconciliation. Separately recheck the staging provider category, Edge secret names and limited database aggregate if a result is uncertain. Record only fixed statuses, the strict database receipt hash and terminal journal metadata; preserve the consumed receipt. Commit the disarmed outcome and update `.agent/HANDOVER.md`. A passing V2 read permits preparation of the **separate** two-field provider-normalization review, not the mutation itself.
